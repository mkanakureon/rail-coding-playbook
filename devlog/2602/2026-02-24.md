# [PROJECT_NAME]開発日誌 — 2026年2月24日

> 30コミット / +43,873行 / -1,882行

## OSSドキュメントの大量整備

取り掛かったのはインタプリタの実装報告書から。そこからOSS公開用のドキュメント24ファイルを一気に追加した。整備計画書、クラス対応設計書、出力モデル仕様書、ConsoleEngine計画書と、いわゆる「外に出すための文書」が中心。コードそのものよりドキュメントの行数のほうが多い日だった。

ConsoleEngineのガイドも追加して、関連ドキュメントもまとめて更新。OSS公開に向けて、読む側の導線を整えるフェーズに入っている。

## IEngineAPIのコンソール実装とTestEngine

`ConsoleEngine` を実装した。IEngineAPIのOSSコンソール実装で、ブラウザもPixiJSも使わずにスクリプトを実行できる。続けて状態管理する `TestEngine` も追加。テストで使うためのエンジン実装で、サンプルスクリプトの整理・拡充も同時に行っている。

エンジンの抽象化がここまで来ると、同じスクリプトが Web でもコンソールでもテストでも動くという状態になる。Switch 移植時にも同じ構造が使える見通し。

## Zenn記事の大量生成

Zenn記事パターン集を作成し、278コミットから投稿ネタ38本を抽出した。予想文字数の付与、Gパターン（メタ）の追加を経て、最終的に39本の記事を一括作成（A-01からG-01まで）。パターン集自体も汎化・簡素化して再利用性を高めた。

OSS同期スキルに感想コメントルールやルートファイル保護ルールを追加するなど、記事生成の品質管理にも手を入れている。save-reportスキルにはモデル名署名ヘッダーを追加した。

## Blueprintの導入とリポジトリ整理

Blueprint方式を導入し、`.bp` ファイル40件と仕様書・レポートを追加した。Zenn記事と開発記も同時に作成。テストファイル一覧の作成（Vitest 33 + Playwright 31 + テンプレート 9）も行い、プロジェクト全体の見通しをよくしている。

リポジトリの整理も進めた。`.gitignore` に `.next/` を追加、`apps/apps/` という重複ディレクトリを削除してgitignoreで再発防止、デプロイログをgit追跡から除外、`apps/data/` を `fixtures/data/` に移動。dev credentialsファイルに警告ヘッダーも追加した。

## 公式アセットのslug問題とデプロイ修正

公式アセットインポート時にslugが長すぎて500エラーになる問題を修正。エディタのアセットフィルターも新しいtaxonomyに対応させた。デプロイ時のビルドエラー4件も修正し、ミニDSL運用仕様とフォルダー整理レポートも残している。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

43,873行の追加というのは、このプロジェクト始まって以来でも最大級の数字ではないか。ただ中身を見ると、ドキュメントとZenn記事の比重が極めて大きい。コードの実装は ConsoleEngine と TestEngine が中心で、エンジンの本体を直接触る変更は少なめ。

OSS公開に向けたドキュメント整備というのは、開発の勢いからすると少し地味に映るかもしれないが、実際には設計の理解度が問われる作業。24ファイルのドキュメントを書くには、自分のコードを改めて外部の視点で読み直す必要がある。その過程でインターフェースの説明が曖昧だった箇所や、暗黙の前提に気づくことも多い。

Blueprint方式の導入は興味深い。`.bp` ファイルが40件という数は、プロジェクトの構造をかなり細かくブループリントとして記述していることを示している。設計意図を明示的にファイルとして残すアプローチで、新しいコンポーネントを追加する際の指針になる。

Zenn記事を278コミットから38本抽出するという作業は、コミットログをコンテンツの原資として扱うという発想。1コミットが1記事になるわけではなく、関連するコミット群をまとめてひとつの記事テーマに仕上げている。この粒度の判断が記事の読みやすさを左右する。

リポジトリ整理で `apps/apps/` という重複ディレクトリが見つかったのは、モノレポ運用の落とし穴。ディレクトリ構造のミスはIDEのファイルツリーでは気づきにくく、CIやビルドスクリプトが正常に動いていると表面化しにくい。gitignoreで再発防止しているのは正しい判断。

slugの長さ制限によるインポートエラーは、データベースのカラム制約がアプリケーション層まで伝播する典型的なパターン。エラーメッセージが500という素っ気ないステータスコードだけでは、原因特定に時間がかかったかもしれない。

　　　　　　　　　　Claude Opus 4.6
