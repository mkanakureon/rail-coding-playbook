# [PROJECT_NAME]開発日誌 — 2026年2月10日

> 5コミット / +1,654行 / -469行

## コンパイラの型修正と復旧

前日のブラウザ統合で見つかった問題を処理する日。`ScriptCommand` 型を `Op` 型に修正してコンパイラを復旧させた。2日前の Op 統一で `ScriptCommand` は廃止したはずだが、コンパイラの出力型が追従できていなかった。型が合わないとエンジンに食わせられない。地味だが放置すると全体が止まる類のバグ。

## デモタイトルの自動同期と manifest 管理

デモタイトル向けに自動同期機能と manifest 管理システムを実装した。`.ks` ファイルを編集してコンパイルしたら、デモ側に自動で反映される仕組み。手動でコピーする手間が消えると、スクリプトの修正→確認のサイクルが格段に速くなる。`phase1-demo.ks` のテスト環境整備とドキュメント化も同時に進めている。

## コンパイル・配信ワークフローのドキュメント化

`.ks` ファイルのコンパイルから配信までの一連のワークフローを総合ドキュメントとしてまとめた。コンパイラの使い方、出力フォーマット、配信サーバーへのデプロイ手順。コードを書く日とドキュメントを書く日が交互に来るのではなく、同じ日に両方やるのがこのプロジェクトのスタイル。コンパイル済みシナリオも最新版に更新して、デモがすぐに動く状態を維持。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

前3日の爆発的な開発量に比べると、5コミット・1,654行は穏やかな日。しかし、こういう日が実は大事だったりする。型の不整合を直し、自動同期を入れ、ドキュメントを整理する。華やかさはないが、これをやらないと翌日以降の開発が滞る。

コンパイラの `ScriptCommand` → `Op` 型修正は典型的な「リファクタリングの漏れ」。大きな型変更をしたときに、依存するすべてのモジュールが追従できているかを確認するのは意外と難しい。TypeScript の型チェックが通っていても、ランタイムで型が違うということはある（`as` キャストを使っていたり、`any` が挟まっていたりする場合）。今回はコンパイラの出力型が古いままだったので、エンジンに渡した時点で壊れた。

自動同期の仕組みは開発体験（DX）に直結する。ファイルを保存したら自動でコンパイルされてデモに反映される。Vite の HMR と合わせれば、ブラウザをリロードすることすら不要になる可能性がある。ビジュアルノベルのスクリプトは試行錯誤の連続なので、フィードバックの速さは品質に直結する。

削除行が469行あるのは、不要になったコードや古い型定義をちゃんと消しているということ。「動いているから触らない」ではなく、不要なものは消す。コードベースが小さいうちにこの習慣をつけておくと、あとで楽になる。

　　　　　　　　　　Claude Opus 4.6
