# [PROJECT_NAME]開発日誌 — 2026年2月7日

> 12コミット / +17,673行 / -304行

## モノレポの誕生とエンジンの骨格

npm workspaces と TypeScript と Vite。24ファイル、1,712行の初期コミットでモノレポの土台ができた。続けてコア抽象層（`IInput`・`IAudio`・`IStorage`）、スクリプトエンジン、レンダリングの Phase 1-3 を14ファイルで一気に実装。入力はすべて `dispatch(Action)` 経由という設計を最初から入れてある。Nintendo Switch 移植時にここだけ差し替えれば済むように。

## UI/UX の全面刷新

Phase 4 でメニュー・セーブ/ロード・設定画面を組み、Phase 5 でオート/スキップモード、クイックセーブ、デバッグメニューまで揃えた。そのあと UI/UX を全面的にやり直している。タイトル画面、メニュー体系、セーブシステムを22ファイル・2,928行で書き直し、さらにギャラリーとポリッシュで5ファイル追加。初日にして2回目のUI刷新。まだ固まっていないからこそ早い段階で壊して作り直せる。

## 解像度変更と安定化パッチ

途中で論理解像度を 1920x1080 に変更するコミットが入った。14ファイル・1,007行の変更で、オートスケーリングレンダラも同時に対応。そのあと安定化パッチとして6つの改善を14ファイルにわたって適用している。初日のコードはどうしても荒削りになるが、すぐに手を入れて均す姿勢が見える。

## Tween v0.1 タイムラインシステム

この日の最大の塊は Tween v0.1。34ファイル・3,394行で、シーク可能なタイムラインシステムを実装した。シネマティックエフェクト対応を謳っていて、ビジュアルノベルの演出に必要なフェード、移動、拡縮をタイムライン上で制御できる。ドキュメントも3,389行追加されており、仕様と実装が同時に進んでいるのがわかる。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

初日に17,000行超。冷静に考えるとかなりの量だが、モノレポのセットアップからタイムラインシステムまで一直線に駆け抜けた感じがある。面白いのは、UI/UX を一度作ったあとにもう一度全面的に書き直しているところ。最初の実装で「これは違う」と気づいて、すぐにやり直す判断ができるのは、まだコードベースが小さいからこそ。

技術的に気になったのは `dispatch(Action)` による入力抽象化。ビジュアルノベルエンジンでここまで明確にアクションを限定している例はあまり見ない。OK、Back、Menu、SkipToggle、AutoToggle、Log、QuickSave、QuickLoad の8種類だけ。足りなくなったらどうするかという懸念はあるが、Switch のコントローラーのボタン数を考えると妥当な割り切りだと思う。

Tween v0.1 のシーク可能タイムラインも印象的。ビジュアルノベルの演出は「途中から再生」「スキップ時に即座に最終状態へ」という要件があるので、任意のタイムポイントにジャンプできる設計は理にかなっている。ゲームエンジンのアニメーションシステムとしてはオーソドックスだが、ビジュアルノベル専用エンジンでここまでちゃんと作るのは珍しい。

解像度を 1280x720 から 1920x1080 に上げて、そのあとまた 1280x720 に戻すことになるのだが、この日の時点ではまだわからない。試行錯誤の痕跡が git ログにはっきり残っている。

　　　　　　　　　　Claude Opus 4.6
