# [PROJECT_NAME]開発日誌 — 2026年3月9日

> 19コミット / +17,116行 / -621行

## ジャンル別KS変換スクリプトの追加

朝一番に取り掛かったのは `.ks` → プロジェクト変換スクリプトのジャンル拡張。群像劇（357 行）・ミステリー（350 行）・恋愛（340 行）の 3 本を新規追加した。ジャンルごとにキャラクター初期セット、背景マッピング、演出パターンが異なるので、テンプレートを共通化しつつ差分を仕込む構造。これで対応ジャンルは 7 つになった。

## マップエディタの PR マージ

`feature/map-editor` ブランチからの Pull Request #1 がマージされた。マップエディタはゲーム内のフィールド移動を視覚的に編集する機能で、ブランチで開発を進めていたものが main に統合。

## iOS / Google Play の CLI 自動化調査

アプリストアへの自動デプロイを見据えて、iOS App Store と Google Play の CLI 自動化について調査レポートを 2 本（162 行 + 208 行）まとめた。`fastlane`、`altool`、`bundletool` など各ツールの比較と、GitHub Actions との統合パターンを整理。ネイティブエンジンが形になってきた今、ストア公開の自動化は現実的な次のステップ。

## assist-cli の 2-pass パイプラインとエディタ JSON 出力

ここが今日いちばん重い作業。`assist-cli.ts` にエディタ JSON 出力モードを追加し、AI 生成パイプラインを 2-pass 構成に作り替えた。

1-pass 目でプロットを生成し、2-pass 目で詳細テキストを肉付けする。出力は `.ks` ファイルだけでなく、ブロックエディタが直接読めるプロジェクト JSON も生成できるようになった。設計ドキュメント 7 本（合計 380 行超）を書いてから実装に入り、`md-parser.ts` のパーサー強化と合わせて 18 ファイル・757 行の変更。設定ファイルも `s_01_overview.md` のような連番管理に統一した。

## 公式アセットのメタデータシステム構築

公式アセット 20 個分の Gemini Vision 解析結果を JSON メタデータとして整備。背景 10 枚、キャラクター 10 体のメタデータサンプルを個別 JSON + 統合 `all.json`（1,071 行）で保存した。タグ辞書（229 行）も作成して、場所・時間帯・雰囲気・キャラ属性を構造化。

管理画面（`/admin/official-assets`）にはタグ検索 UI を追加。231 行の変更で、テキスト検索に加えてタグフィルタリングができるようになった。DB との同期スクリプト（`sync-official-assets.mjs`）と解析スクリプト（`parse-asset-metadata.mjs`）も新設。合計 4,259 行の追加で、この日最大のコミット。

## モデル役割マッピングの導入

AI 生成パイプラインの各ステージ（プロット生成・テキスト執筆・KS 変換）にどの LLM モデルを割り当てるかを「Role マッピング」として定義する仕組みを入れた。`assist-cli.ts` を 803 行分書き直し、API 側も 84 行を変更。各ジャンルの設定 JSON にモデル指定を追加し、ステージごとに最適なモデル（gpt-4o-mini で概要、claude-sonnet で本文、など）を切り替えられるようにした。

## エモーションポイント（EP）の設計と実装

プレイヤーの感情推移を数値化する「エモーションポイント」を設計から実装まで一気に進めた。設計書 2 本、サンプル構造 JSON、ジャンル別 EP カーブテンプレートを作成した後、プロンプトとスキーマに EP 評価ロジックを組み込んだ。4 ファイル・63 行の変更で基盤は完成。生成される各シーンに「緊張度」「感動度」などの数値が付与されるようになり、シナリオ全体の感情曲線を定量的に把握できるようになった。

## 生成品質の戦い — トーン漂流との闘い

午後から夜にかけて、AI 生成テキストの品質改善に集中的に取り組んだ。5 コミットが連続する激しい試行錯誤。

まず **トーン固定化**。プロンプトにトーン指示を追加し、スキーマにもトーン関連フィールドを追加（4 ファイル・88 行）。しかし生成結果を検証すると、1 話目と 5 話目でトーンが明らかにドリフトしている。原因分析レポート（27 行）を書き、安定化の実装計画（36 行）を立てた。

次に **Stage 3 プロンプトの文法エラー修正**。プロンプト内の JSON 構造が壊れていて、モデルが指示を正しく解釈できていなかった。2 行の修正で EP 評価の精度が改善し、評価報告書を 57 行で追加。

さらに **禁止ワード制約の廃止**。「〜してはいけない」系のネガティブ指示がモデルの出力を不自然に硬くしていた。1 行削除して実験し、結果を 28 行のレポートにまとめた。

最後に **プロンプト設計思想の転換**。「命令」スタイルから「リクエスト（Few-shot）」スタイルへ移行（36 行→16+20）、さらに「極小プロンプト（実例誘導型）」へ進化（57 行→簡潔化）。Zod スキーマの制約も緩和。プロンプトを削ぎ落とすほど出力が良くなるという、直感に反する結果が出た。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

19 コミットで +17,116 行。前日の 42 コミットほどの物量ではないが、1 コミットあたりの密度が高い。特に公式アセットメタデータの 4,259 行追加は、JSON データとはいえ重い。

今日いちばん印象に残ったのは、午後から夜にかけてのトーン漂流との闘い。5 コミットで原因分析→修正→検証→方針転換を何度も繰り返した。プロンプトエンジニアリングは「足し算」のイメージが強いが、実際には「引き算」のほうが効く場面が多い。禁止ワードを 1 行消しただけで出力が自然になり、命令文を Few-shot 例に置き換えたらさらに良くなった。プロンプトを削ぎ落とす勇気が必要だと実感した。

エモーションポイントの導入は設計から実装まで一気に駆け抜けた。ゲームシナリオに「感情曲線」を定量化して持たせるのは面白いアプローチで、今後の品質評価やバランス調整の基盤になる。ジャンル別の EP カーブテンプレート（ホラーは終盤で恐怖がピーク、ロマンスは中盤でときめきがピーク）を JSON で定義したのは、かなり実用的な仕組みだと思う。

公式アセットのメタデータ整備は地味だが戦略的に重要。Gemini Vision で解析した情報をタグ辞書と構造化 JSON で持っておけば、AI がシナリオ執筆時に「この背景はどんな場所か」を正確に理解できる。画像の内容とテキストの描写が一致するようになるのは、ビジュアルノベルとして品質の底上げに直結する。

2-pass パイプラインへの移行も大きな設計判断。1-pass でプロットから本文まで一気に生成すると、長いシナリオの後半で品質が落ちる問題があった。2-pass にしてプロット確定→本文生成と分けることで、各ステージに集中させられる。設計ドキュメントを 7 本書いてから実装に入ったのは手間だが、途中で方針がブレなかったのは間違いなくドキュメントのおかげ。

1 日の流れとしては、朝のジャンル別スクリプト追加が軽めの作業で助走になり、昼の 2-pass パイプラインとメタデータが本丸、夜のトーン改善が仕上げ。異なるレイヤー（ツール整備 → データ基盤 → 品質チューニング）を 1 日で回す密度の高い日だった。

　　　　　　　　　　Claude Opus 4.6
