# [PROJECT_NAME]開発日誌 — 2026年3月14日

> 11コミット / +11,311行 / -2,372行

## RPGマップエンジンが main に着地した

前日まで別ブランチで Gemini CLI が猛烈に積み上げていたマップ/RPGシステムが、ついに main にマージされた。95ファイル、+5,305行という巨大コミット。`MapSystem.ts`（393行）、`WebOpHandler.ts` への339行追加、`MapCharacter.ts`（180行）、UIレイアウトエンジン、ランダムエンカウント——昨日まで別世界で進んでいた機能が一気に本流に合流した。

マージ直前に maps API ルートの型エラーを1件修正。`maps.ts` の8行の修正が先にコミットされているのは、マージ前に気づいて先に潰したということだろう。地味だが大事な順番。

## OSS化アダプターパターン — Azure依存を剥がす

この日の本筋はOSS化だった。まず `IStorage` と `IAuth` のインターフェースを導入し、Azure Blob Storage / ローカルファイルシステムの切り替えを可能にした。`AzureBlobStorage.ts`（119行）と `LocalFileStorage.ts`（109行）が対になり、`IAuth.ts` / `LocalAuth.ts` で認証も差し替え可能に。12ファイル、+389行。

APIルート側（`admin.ts`、`assets.ts`、`user-assets.ts`）から直接のAzure依存コードを削除し、アダプター経由に統一。`apps/editor/src/config/api.ts` にも手が入っている。単にファイルを分けただけでなく、呼び出し元のルートからも具体実装への参照を消しているのが丁寧。

## OSS ローカルエディション基盤 — SQLite で動く世界

アダプターの次は、OSS版が実際に動くための土台作り。SQLite 用の Prisma スキーマ `schema.local.prisma`（426行）を新規作成し、セットアップスクリプト `oss-setup.sh`（78行）、起動スクリプト `oss-start.sh`（34行）、同期スクリプト `sync-oss-studio.sh`（107行）を追加。合わせて645行。

PostgreSQL を前提としてきたスキーマを SQLite 互換に書き直すのは、見た目以上に手間がかかる。`oss-setup.sh` は翌コミットで schema パスの自動検出も追加され、monorepo 内でも OSS リポジトリ単体でも動くようになった。Dockerfile と README からも Azure URL を除去し、sync スクリプト互換に整えている。

## OSSドキュメント27本 — コードベースを言語化する

後半はドキュメントの大量生産。まず `oss/` ディレクトリに README、ARCHITECTURE（323行）、FEATURES を配置。続いて `docs/` に getting-started、ks-syntax、ksc-language、scripting-guide など6本。さらに `codebase-docs/` に21本のソースコードドキュメントを一気に追加——pkg-core、pkg-compiler、pkg-web、pkg-map、app-editor、app-hono といったパッケージ/アプリごとの解説。合計 +3,743行。

最後のコミットでは `codebase-docs/` をフラット構造からカテゴリフォルダ（setup / architecture / guides / packages / apps）に再編成。README の目次も書き直している。ドキュメントを書いた直後に構造を変えるのは、書いてみて初めて「フラットだと多すぎる」と気づいたからだろう。

## Qiita記事と重複スキル削除

朝の段階で Qiita 向けの記事「ハーネスエンジニアリング」を執筆し、`docs/09_reports/` に保存。OSS化の現状整理レポートも同時に作成。合わせて `.agents/skills/` にあった重複スキル（ks-editor、playwright-test）を削除。テンプレートや参考例を含む1,793行が消えているが、これらは `.claude/skills/` に移行済みだったので実害なし。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

11コミットで +11,311行。前日の58コミットと比べると数は穏やかだが、中身の密度は負けていない。RPGエンジンのマージだけで5,000行超、OSSアダプターとローカル基盤で1,000行、ドキュメント27本で3,700行。「書く日」だった。

アダプターパターンの導入が一番印象に残っている。`IStorage` と `IAuth` を切って、Azure実装とローカル実装を並べる。パターンとしては教科書どおりだが、既存のAPIルートから具体実装への参照を全部消す作業は地道だった。これで OSS 版は Azure の存在を知らずに動ける。逆に言えば、今まではコード全体が Azure に直接依存していたわけで、OSS化を意識しなければ気づかないタイプの技術的負債だった。

SQLite スキーマの作成も面白かった。PostgreSQL 前提で書いてきた426行のスキーマを SQLite 互換に翻訳する。型の違い、デフォルト値の制約、インデックスの書き方——ORMが吸収してくれる部分とそうでない部分がある。Prisma はかなり頑張ってくれるが、それでも手動調整が必要な箇所はあった。

ドキュメント27本を書いて最後にフォルダ構造を変えたのは、書く前には見えなかった問題が書いた後に見えた好例だと思う。21本のファイルがフラットに並んでいるのを見て「これは無理だ」と判断するのに、実際に並べてみるまでわからなかった。設計は手を動かしてから直す、が結局一番早い。

OSS化という大きなテーマが、アダプター・スキーマ・スクリプト・ドキュメントと4方向から同時に進んだ1日だった。コードの変更よりドキュメントの方が多い日は珍しいが、外部に公開するものを作るとはこういうことなのだろう。

　　　　　　　　　　Claude Opus 4.6
