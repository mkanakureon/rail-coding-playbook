# 2026-03-23 実装分 テスト計画書

> Generated by Claude Opus 4.6

## 対象コミット

20コミット。コード変更を含むもの（docs のみは除外）を対象にテストする。

---

## A. 自動テスト（既に実施済み）

| # | テスト | 結果 | ファイル |
|---|--------|------|---------|
| A-1 | コンパイラ: filter_mix 同期テスト | 272/272 通過 | `packages/compiler/test/command-sync.test.ts` |
| A-2 | コンパイラ: filter_mix 出力値テスト（2〜4層、奇数引数） | 通過 | 同上 |
| A-3 | コンパイラ: filter_mix エラーケース（引数不足） | 通過 | 同上 |
| A-4 | lineClassifier: filter_mix を needsArgs に追加 | 通過 | `packages/compiler/test/lineClassifier.test.ts` |
| A-5 | 型チェック: 全5パッケージ | 全通過 | tsc --noEmit |

## B. 自動テスト（追加が必要）

### B-1. タイトル画像修正の回帰テスト

| # | テスト | ファイル | 内容 |
|---|--------|---------|------|
| B-1-1 | サムネイル URL が /storage/assets/ で始まる | `tests/shared/editor/` 新規 | API `/api/works/:id/thumbnails` の url フィールドが正しいパス |
| B-1-2 | 画像ファイルが HTTP 200 で取得できる | 同上 | `resolveAssetUrl` → 実 URL → fetch で 200 確認 |
| B-1-3 | 撮影ボタンが「実行画面から選択」後に表示される | `tests/shared/editor/` 新規 | Playwright で手動キャプチャ → 撮影ボタン visible |

### B-2. OTel トレーシング

| # | テスト | ファイル | 内容 |
|---|--------|---------|------|
| B-2-1 | OTEL_ENABLED=false 時にオーバーヘッドなし | `apps/hono/test/` 新規 | API レスポンスタイムが OTEL 無効時と有意差なし |
| B-2-2 | tracingMiddleware が http.status_code を記録 | 同上 | モック exporter で span 属性を検証 |

### B-3. セキュリティ修正

| # | テスト | ファイル | 内容 |
|---|--------|---------|------|
| B-3-1 | パストラバーサル拒否（thumbnails） | `apps/hono/test/` 新規 | `validatePathParam("../etc/passwd")` → エラー |
| B-3-2 | パストラバーサル拒否（/storage/*） | 同上 | `GET /storage/../etc/passwd` → 403 |
| B-3-3 | null バイト拒否 | 同上 | `validatePathParam("file\0.txt")` → エラー |
| B-3-4 | 動画生成の非同期実行 | 同上 | execSync が使われていないことを grep で確認 |

### B-4. 認証・リミット

| # | テスト | ファイル | 内容 |
|---|--------|---------|------|
| B-4-1 | API 件数リミット（limit=9999 → 100） | `apps/hono/test/` 新規 | `GET /api/projects?limit=9999` → 結果が100件以下 |
| B-4-2 | 二段構え認証（停止ユーザーの公開ブロック） | 同上 | status=suspended のユーザーが publish → 403 |
| B-4-3 | Health Dashboard API | 同上 | `GET /api/admin/health-dashboard` → db, memory, uptime が返る |

### B-5. Discord リンク

| # | テスト | ファイル | 内容 |
|---|--------|---------|------|
| B-5-1 | VITE_DISCORD_URL 設定時にエディタにリンク表示 | `tests/shared/editor/` 新規 | Header に「質問する」ボタンが visible |
| B-5-2 | NEXT_PUBLIC_DISCORD_URL 設定時に Next.js にリンク表示 | `tests/shared/flow/` 新規 | Header に「質問する」リンクが visible |
| B-5-3 | 環境変数未設定時はリンク非表示 | 同上 | ボタンが存在しない |

### B-6. フィルター（既存テスト拡張）

| # | テスト | ファイル | 内容 |
|---|--------|---------|------|
| B-6-1 | filter_mix 制約バリデーション警告 | `tests/shared/editor/filter-categories.spec.ts` 追加 | 天候系2つ選択 → 黄色警告テキスト表示 |
| B-6-2 | フィルターカテゴリ判定 | `apps/editor/src/__tests__/` 新規 | `getFilterCategory('rain')` → 'weather' 等 |

---

## C. 手動確認（ブラウザ）

### C-1. タイトル画像

| # | 確認項目 | 手順 | 期待結果 |
|---|---------|------|---------|
| C-1-1 | 画像一覧表示 | プロジェクト詳細ページを開く | サムネイル画像が正常に表示（リンク切れなし） |
| C-1-2 | クリックでタイトル設定 | 画像一覧のサムネイルをクリック | 「タイトル画像を設定しました」トースト + 「使用中」ラベル |
| C-1-3 | 撮影ボタン表示 | 「実行画面から選択」→ プレビューウィンドウ表示後 | 「撮影」ボタンが表示される |
| C-1-4 | 画像なしフォールバック | 存在しない URL のタイトル画像 | 「画像なし」テキスト表示 |

### C-2. フィルター

| # | 確認項目 | 手順 | 期待結果 |
|---|---------|------|---------|
| C-2-1 | 新規22フィルター選択 | エディタでフィルターブロック → ドロップダウン | 7カテゴリ・全43フィルターが選択可能 |
| C-2-2 | フィルター効果のプレビュー | フィルター選択 → リロードボタン | プレビューに色調変化が反映 |
| C-2-3 | filter_mix ブロック追加 | ブロック追加メニュー → 重ね掛け | 2層のセレクトが表示 |
| C-2-4 | filter_mix 層操作 | + ボタン / - ボタン | 2〜4層の範囲で増減 |
| C-2-5 | filter_mix 制約警告 | 天候系を2つ選択 | 黄色テキストで警告表示 |

### C-3. OTel

| # | 確認項目 | 手順 | 期待結果 |
|---|---------|------|---------|
| C-3-1 | Jaeger にトレース表示 | Docker + Jaeger 起動 → API リクエスト → Jaeger UI | Service: kaedevn-api でトレース表示 |
| C-3-2 | otel-viewer でセッション解析 | `npx tsx src/index.ts stats` | セッション統計が出力 |

### C-4. セキュリティ

| # | 確認項目 | 手順 | 期待結果 |
|---|---------|------|---------|
| C-4-1 | /storage/../../etc/passwd | curl でアクセス | 403 Forbidden |
| C-4-2 | Health Dashboard（非admin） | 一般ユーザーでアクセス | 403 |
| C-4-3 | Health Dashboard（admin） | admin でアクセス | DB latency, memory, uptime が返る |

### C-5. Discord リンク

| # | 確認項目 | 手順 | 期待結果 |
|---|---------|------|---------|
| C-5-1 | エディタのヘッダー | VITE_DISCORD_URL 設定 → エディタ表示 | 「💬 質問する」ボタンが表示 |
| C-5-2 | Next.js のヘッダー | NEXT_PUBLIC_DISCORD_URL 設定 → サイト表示 | 「💬 質問する」リンクが表示 |
| C-5-3 | リンク先 | ボタンクリック | 新しいタブで Discord 招待ページが開く |

---

## D. 実施優先度

| 優先度 | テスト | 理由 |
|--------|--------|------|
| **高** | B-3（セキュリティ） | パストラバーサルは脆弱性 |
| **高** | B-4（認証・リミット） | 停止ユーザーの公開ブロック |
| **高** | C-1（タイトル画像） | ユーザー影響直結 |
| **中** | B-6 + C-2（フィルター） | 新機能の品質確認 |
| **中** | C-4（セキュリティ手動） | 自動テストの補完 |
| **低** | B-2（OTel） | 開発ツール |
| **低** | B-5 + C-5（Discord） | 環境変数未設定では影響なし |

## E. テスト実行コマンド

```bash
# コンパイラテスト（既に通過済み）
cd packages/compiler && npx vitest run

# E2E テスト（フィルター）
npx playwright test tests/shared/editor/filter-categories.spec.ts --config=tests/configs/playwright.local.config.ts

# E2E テスト（全体）
npx playwright test --config=tests/configs/playwright.local.config.ts

# セキュリティ手動テスト
curl -sI "http://localhost:8080/storage/../../etc/passwd"
curl -s "http://localhost:8080/api/admin/health-dashboard" -H "Authorization: Bearer $TOKEN"
```
