# 2026-03-23 テスト実行レポート

> Generated by Claude Opus 4.6

## 背景

リリース前のテスト計画書（`09-test-plan.md`）に基づき、自動テストの実行とバグ修正を行った。新規実装はバグ修正のみとし、機能追加は行わない方針。

## テスト実行結果

### A. 自動テスト（既存）— 全通過

| # | テスト | 結果 | 件数 |
|---|--------|------|------|
| A-1〜A-4 | コンパイラ (filter_mix 含む) | 全通過 | 272/272 |
| A-5 | 型チェック (全パッケージ) | 全通過 | TS5055 は既存の dist 警告のみ |

### B-3. セキュリティ — 全通過（既存テスト）

| # | テスト | 結果 |
|---|--------|------|
| B-3-1 | パストラバーサル拒否 (`../etc/passwd`) | 通過 |
| B-3-2 | パストラバーサル拒否 (途中 `path/../secret`) | 通過 |
| B-3-3 | null バイト拒否 (`file\0.txt`) | 通過 |
| B-3-4 | 動画生成の非同期実行 (execSync 不使用) | 通過 |

テストファイル: `apps/hono/test/validation.test.ts`

### B-4. 認証・リミット — 全通過（既存テスト）

| # | テスト | 結果 |
|---|--------|------|
| B-4-1 | Health Dashboard 認証なし → 401 | 通過 |
| B-4-2 | limit パラメータ受付確認 | 通過 |
| B-4-3 | 不正トークン拒否 → 401 | 通過 |

テストファイル: `apps/hono/test/admin-api.test.ts`

### B-6. フィルター — 全通過（既存テスト）

| # | テスト | 結果 |
|---|--------|------|
| B-6-2 | `getFilterCategory` ユニットテスト (11ケース) | 通過 |

テストファイル: `apps/editor/src/__tests__/utils/filterCategories.test.ts`

### 全パッケージ結果サマリ

| パッケージ | ファイル数 | テスト数 | 結果 |
|-----------|-----------|---------|------|
| hono | 48 (+ 6 skipped) | 421 (+ 116 skipped) | 全通過 |
| compiler | 9 | 272 | 全通過 |
| editor | 12 | 278 | 全通過 |
| core | 10 | 217 | 全通過 |

## 発見・修正したバグ（2件）

### バグ1: `filter_mix` が `COMMAND_DEFINITIONS` に未登録

- **症状**: `commandSync.test.ts` で `"filter_mix" が COMMAND_DEFINITIONS に未定義` エラー
- **原因**: コンパイラ側（`COMMAND_PARSERS`）に `filter_mix` が追加されたが、core 側の `COMMAND_DEFINITIONS` への同期が漏れていた
- **修正**:
  - `packages/core/src/registry/commandDefinitions.ts` — `filter_mix` エントリを追加（opType: `FILTER_MIX`, handlerMethod: `filterMix`, category: `filter`）
  - `packages/core/test/commandSync.test.ts` — `validMethods` 配列に `filterMix` 追加、コマンド数を 36→37 に更新

### バグ2: プリセットテストが `smartphone-portrait` を含んでいない

- **症状**: `presets.test.ts` で `expected 11 to be 10` エラー
- **原因**: `smartphone-portrait` プリセットが `UI_PRESETS` に追加されたが、テストの `EXPECTED_PRESETS` 配列と期待値が未更新
- **修正**:
  - `packages/core/test/presets.test.ts` — `EXPECTED_PRESETS` に `smartphone-portrait` 追加、期待値を 10→11 に更新

## 変更ファイル一覧

| # | ファイル | 変更 |
|---|---------|------|
| 1 | `packages/core/src/registry/commandDefinitions.ts` | `filter_mix` コマンド定義を追加 |
| 2 | `packages/core/test/commandSync.test.ts` | `filterMix` を validMethods に追加、コマンド数 36→37 |
| 3 | `packages/core/test/presets.test.ts` | `smartphone-portrait` を EXPECTED_PRESETS に追加、11種に更新 |

## 未実施テスト（手動確認・E2E が必要）

| 優先度 | テスト | 理由 |
|--------|--------|------|
| **高** | C-1 タイトル画像 (ブラウザ手動確認) | サーバー起動が必要 |
| **中** | B-6-1 filter_mix 制約警告 (E2E) | Playwright + エディタ起動が必要 |
| **中** | C-2 フィルター操作 (ブラウザ手動確認) | サーバー起動が必要 |
| **中** | C-4 セキュリティ手動 (curl) | API サーバー起動が必要 |
| **低** | B-2 OTel トレーシング | 開発ツール、Docker + Jaeger 必要 |
| **低** | B-5 / C-5 Discord リンク | 環境変数未設定時は影響なし |

## 教訓

- **コマンド追加時の同期チェックリスト**が CLAUDE.md にあるが、`COMMAND_DEFINITIONS` への登録は実際に漏れやすい。同期テスト（`commandSync.test.ts`）がこの種のバグを確実に検出している点は良い設計
- プリセット追加時もテストの期待値更新が漏れやすい。新しいプリセットを追加する PR ではテスト更新を含めるべき
