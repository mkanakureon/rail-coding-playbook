# ノベル CLI 総合テスト結果 + 改善計画

> Generated by Claude Opus 4.6

---

## 1. テスト結果

### 実施したテスト

| # | テスト | 結果 |
|---|--------|:----:|
| 1 | `init-project --config fantasy` でプロジェクト作成 | ✅ |
| 2 | `blocks` でブロック一覧表示 | ✅ |
| 3 | `add` で各種ブロック追加（text, effect, choice, jump, set_var, if, screen_filter） | ✅ |
| 4 | `update` でテキスト編集 | ✅ |
| 5 | `move` でブロック並替 | ✅ |
| 6 | `validate` でバリデーション | ✅ |
| 7 | `snapshot` / `snapshots` でスナップショット | ✅ |
| 8 | `template list` | ✅（テンプレート未定義は正常） |
| 9 | `import-story --preview` で台本プレビュー | ✅ |
| 10 | `import-story` で台本インポート | ✅ |
| 11 | `test-blocks.mjs --create` 自動テスト（62テスト） | ✅ 全通過 |
| 12 | `character-cli list` でキャラ一覧 | ✅ |
| 13 | `asset-cli list` でアセット一覧 | ✅ |
| 14 | `test-project.mjs` でプロジェクト一括検証 | ⚠️ 2件エラー |

### 発見した問題

| # | 問題 | 重大度 | 原因 |
|---|------|:------:|------|
| 1 | **import-story で `@bg slug` がそのまま assetId に入る** | 中 | slug → Asset ID の解決がインポート時に行われない。`@bg bg_field` → assetId="bg_field" となるが、実際のIDは ULID |
| 2 | `init-project` で既存キャラID被りが 409 エラー | 低 | 既存プロジェクトと共有の公式キャラ。スキップされるので実害なし |

**バグは実質1件**（import-story の slug 未解決）。それ以外は全て正常動作。

---

## 2. CLI の現状評価

### 充実しているもの

| 機能 | CLI | 状態 |
|------|-----|:----:|
| プロジェクト作成（ジャンル別テンプレート） | `init-project` | ✅ 完成度高 |
| ブロック CRUD（全14型） | `editor-cli` | ✅ 完成度高 |
| 台本インポート | `import-story` | ⚠️ slug 解決に問題 |
| キャラクター管理 | `character-cli` | ✅ 完成度高 |
| アセット管理 | `asset-cli` | ✅ 完成度高 |
| バリデーション | `validate` + `test-blocks` | ✅ 完成度高 |
| スナップショット/復元 | `snapshot` / `restore` | ✅ 完成度高 |

### 不足しているもの

| # | 機能 | 内容 | 効果 |
|---|------|------|------|
| 1 | **import-story の slug → ID 解決** | `@bg bg_field` を実際の Asset ID に変換 | import-story で作ったプロジェクトがバリデーション通る |
| 2 | **`editor-cli context` の拡充** | 利用可能な assetId, characterId, pageId の一覧を見やすく | Claude Code がブロック追加時に正しい ID を使える |
| 3 | **`editor-cli duplicate`** | ブロックを複製 | 似たセリフの量産に便利 |
| 4 | **`editor-cli bulk-add`** | JSON ファイルから複数ブロックを一括追加 | 1つずつ `add` するより効率的 |
| 5 | **`editor-cli preview-text`** | プロジェクトのテキストだけを抽出して表示 | シナリオの通し読み確認 |
| 6 | **`editor-cli stats`** | ページ数、ブロック数、テキスト文字数、使用キャラ一覧 | 進捗把握 |
| 7 | **`editor-cli search`** | ブロック内のテキストを横断検索 | 「このセリフどこにあったっけ」 |

---

## 3. 改善計画

### Phase 1: バグ修正（即時）

| # | 内容 | ファイル | 工数 |
|---|------|---------|:----:|
| 1 | import-story の `@bg slug` を Asset ID に解決 | `scripts/cli/project/import-story.ts` | 小 |

**方法**: インポート前に `GET /projects/:id` で `_ai_context.availableAssets` を取得し、slug → ID のマッピングを作る。`@bg`, `@overlay` の引数を変換する。`@ch` の slug は既にキャラクターのslug として正しいので変換不要。

### Phase 2: 便利コマンド追加（優先順）

| # | コマンド | 内容 | 工数 |
|---|---------|------|:----:|
| 2 | `editor-cli stats` | ページ/ブロック/文字数/キャラ使用率 | 小 |
| 3 | `editor-cli preview-text` | テキストブロックだけ抽出して通し読み | 小 |
| 4 | `editor-cli search` | ブロック内テキスト横断検索 | 小 |
| 5 | `editor-cli duplicate` | ブロック複製 | 小 |
| 6 | `editor-cli bulk-add` | JSON からブロック一括追加 | 中 |

### Phase 3: テスト強化

| # | 内容 | 工数 |
|---|------|:----:|
| 7 | import-story → validate の統合テスト追加 | 小 |
| 8 | init-project → import-story → validate のフルパイプラインテスト | 中 |

---

## 4. 実装順序

```
Phase 1: バグ修正（import-story slug 解決）
    ↓
Phase 2: 便利コマンド（stats → preview-text → search → duplicate → bulk-add）
    ↓
Phase 3: テスト強化
```

---

## 5. テスト環境情報

| 項目 | 値 |
|------|-----|
| API サーバー | localhost:8080（起動済み） |
| テストプロジェクト | 01KMM4JPZFR8T07YV9KARV68JD |
| 自動テスト結果 | 62/62 通過（test-blocks.mjs） |
| ノベル CLI コマンド総数 | 約30（editor-cli 18 + character-cli 7 + asset-cli 12 + init-project + import-story） |
