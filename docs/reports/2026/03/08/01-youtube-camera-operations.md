# YouTube 投稿情報: カメラ操作 E2E テスト

## 動画ファイル

`/Users/kentaromukunasi/Movies/2026-03-08 00-16-43.mov`

## タイトル案

```
【AI自律開発】Claude Opus 4.6 がエディタでカメラ操作 — ズーム1.8倍・パン(X200,Y-100)・プレイ確認まで全自動
```

## 説明文案

```
AI（Claude Opus 4.6）がビジュアルノベルエンジン「kaedevn」のブロックエディタでカメラブロックを追加・操作する E2E テストを自律実行する様子を録画しました。

■ テスト内容
1. API でプロジェクトを新規作成（自動でファンタジーアセット付き）
2. マイページからプロジェクトを選択（トークン注入でログイン省略）
3. エディタを開く
4. カメラブロックを追加（「+ ブロック追加」→「カメラ」）
5. ズーム（Z）を 1.00 → 1.80 に変更
6. X座標を 0 → 200 に変更（右にパン）
7. Y座標を 0 → -100 に変更（上にパン）
8. 実行ボタンからプレイ画面を起動 → シナリオ完走

■ 特徴
- ログイン省略: API でトークン取得 → localStorage 注入でマイページ直接アクセス
- カメラブロック: エディタのプロパティパネル（SliderRow）で Zoom / X / Y を操作
- input の属性セレクタで正確に特定: Z → step="0.01" max="2", X → max="640", Y → max="360"
- OBS テキスト連携: 各フェーズ・クリックごとに操作内容を表示
- プレビュー確認: 変更後に実行してカメラのズーム・パンをプレイ画面で確認

■ 技術スタック
- エンジン: kaedevn（PixiJS / WebGL ベースのビジュアルノベルエンジン）
- エディタ: React + Zustand（Vite, port 5176）
- プレビュー: PixiJS OpRunner（Vite, port 5175）
- API: Hono + Prisma + PostgreSQL（port 8080）
- フロントエンド: Next.js（port 3000）
- テスト: Playwright（headless: false で実行画面を表示）
- OBS 連携: obs-websocket-js でテキストオーバーレイを動的更新
- AI: Claude Opus 4.6（Claude Code CLI）

■ カメラシステム
- sceneLayer（背景・キャラクター・オーバーレイ）にカメラ操作を適用
- UIレイヤーはカメラの影響を受けない
- パラメータ: X(-640〜640), Y(-360〜360), Zoom(0.5〜2.0), シェイク(0〜20)
- アニメーション: イージング付き（Linear / EaseIn / EaseOut / EaseInOut）

■ リポジトリ
kaedevn-monorepo（プライベート）
```

## タグ案

```
AI開発, Claude, Claude Opus, ビジュアルノベル, ゲームエンジン, E2Eテスト, Playwright, 自律開発, kaedevn, PixiJS, React, TypeScript, 自動テスト, AI coding, Claude Code, OBS, カメラ操作, ズーム, パン
```

## テスト仕様

| 項目 | 値 |
|------|-----|
| テストファイル | `tests/camera-operations.spec.ts` |
| 設定ファイル | `playwright.camera.config.ts` |
| エディタ viewport | 1200 x 900 |
| プレイ画面 viewport | 600 x 440 |
| ログイン | 省略（トークン直接注入） |
| ズーム変更 | 1.00 → 1.80 |
| X座標変更 | 0 → 200 |
| Y座標変更 | 0 → -100 |
| プレイクリック数 | 1 |
| 所要時間 | 約1.5分 |
| 終了判定 | コンソール「Scenario completed」検出 |

## フェーズ構成

| Phase | 内容 |
|-------|------|
| 1 | API でプロジェクト作成 |
| 2 | マイページ → プロジェクト選択（トークン注入） |
| 3 | エディタを開く |
| 4 | カメラブロック追加 |
| 5 | ズームを 1.80 に変更 |
| 6 | X を 200 に変更（右パン） |
| 7 | Y を -100 に変更（上パン） |
| 8 | 実行 → プレイ画面でシナリオ完走 |

## プロパティ入力の特定方法

| プロパティ | セレクタ | 理由 |
|-----------|---------|------|
| ズーム (Z) | `input[type="number"][step="0.01"][max="2"]` | step=0.01, max=2 はズーム専用 |
| X座標 | `input[type="number"][max="640"]` | camera ブロックの xMax=640 |
| Y座標 | `input[type="number"][max="360"]` | camera ブロックの yMax=360 |

## 録画履歴

| 時刻 | ファイル | 備考 |
|------|---------|------|
| 00:16 | `2026-03-08 00-16-43.mov` | 最終版（Zoom 1.80, X 200, Y -100, マイページから） |
