# テスト対象チェックリスト（2026-03-20〜22 実装分）

作成日: 2026-03-22
最終テスト: 2026-03-22 Playwright 自動テスト

---

## 1. タイトル画像

### 1-1. 画像アップロード
- [ ] プロジェクトページでタイトル画像をアップロードできる（※要手動テスト: multipart POST）
- [ ] 1MB 以上の画像はエラーになる
- [ ] 画像以外のファイルはエラーになる
- [ ] アップロード上限3枚を超えるとエラーになる
- [ ] アップロード後、現在のタイトル画像が緑枠で表示される

### 1-2. 自動キャプチャ（📸 自動生成）
- [ ] ボタンクリックでプレビューウィンドウが開く（※要手動テスト: popup）
- [ ] 5-6秒後に自動でキャプチャされる
- [ ] キャプチャ画像がサムネイル API にアップロードされる
- [ ] titleImage が更新される
- [ ] ポップアップブロック時にエラーメッセージが出る

### 1-3. 手動キャプチャ（🎮 実行画面から選択）
- [ ] ボタンクリックで実行画面が開く（※要手動テスト: popup）
- [ ] 好きな場面まで進めてから「📷 撮影」ボタンが表示される
- [ ] 撮影ボタンクリックでキャプチャされる
- [ ] 実行画面が閉じられている場合エラーメッセージが出る

### 1-4. 画像一覧
- [x] アップロード・キャプチャした画像が一覧表示される（2026-03-22 Playwright）
- [ ] 画像クリックでタイトル画像に設定される（※要手動テスト）
- [ ] 「使用中」バッジが正しく表示される
- [ ] DL リンクで画像をダウンロードできる
- [ ] 削除ボタンで画像を削除できる
- [ ] 削除後、別の画像がタイトルに自動設定される

### 1-5. サムネイル管理ページ
- [x] `/projects/[id]/thumbnails` にアクセスできる（2026-03-22 Playwright）
- [ ] 一覧表示・タイトル設定・ダウンロード・削除が動作する（※要手動テスト）
- [ ] 「← プロジェクトに戻る」リンクが機能する

### 1-6. サムネイル API
- [x] `GET /api/works/:id/thumbnails` — 一覧取得（2026-03-22 Playwright）
- [x] projectId でも workId でもアクセスできる（2026-03-22 Playwright）
- [ ] `POST /api/works/:id/thumbnails/upload` — アップロード成功（※要手動テスト: トークン + multipart）
- [ ] `PUT /api/works/:id/thumbnails/:id/activate` — タイトル切替
- [ ] `DELETE /api/works/:id/thumbnails/:id` — 削除
- [ ] 他人の作品にはアクセスできない（403）
- [ ] 最大3枚制限が機能する（古いものを自動削除）

### 1-7. 公開時の自動キャプチャ
- [ ] `POST /api/works/:id/publish` でサムネ未設定時に自動キャプチャが走る（※要統合テスト）
- [ ] Playwright が使えない環境では静かにスキップ
- [ ] 公開レスポンスはブロックされない（非同期）

---

## 2. 動画クリップ生成

### 2-1. プロジェクトページ UI
- [x] 動画セクションが表示される（2026-03-22 Playwright）
- [x] 「🎬 動画を生成」ボタンが存在する（2026-03-22 Playwright）
- [x] 9:16（縦）/ 16:9（横）のアスペクト比切替がある（2026-03-22 Playwright）
- [ ] 秒数入力（5-120秒）が機能する（※要手動テスト）
- [ ] 生成中は「生成中...」表示になる（※要統合テスト: Playwright + ffmpeg）
- [ ] 生成済み動画がリストに表示される
- [ ] ステータス表示（完了 / エラー / 生成中）が正しい

### 2-2. 動画 API
- [ ] `POST /api/works/:id/video` — 生成開始（202 返却）（※要統合テスト）
- [ ] duration / aspectRatio パラメータが反映される
- [ ] 処理中に再リクエストすると 409
- [ ] `GET /api/works/:id/video` — 全動画一覧を返す
- [ ] `DELETE /api/works/:id/video/:videoId` — 動画削除
- [ ] ファイルも一緒に削除される
- [ ] projectId でもアクセスできる

### 2-3. 動画生成 CLI
- [ ] `npx tsx scripts/cli/video/generate-clip.ts <workId>` で MP4 が生成される（※要統合テスト: Playwright + ffmpeg）
- [ ] `--duration` で秒数指定できる
- [ ] `--aspect 16:9` で横画面動画が生成される
- [ ] `--aspect 9:16` で縦画面動画が生成される（デフォルト）
- [ ] clipDuration 超過で自動停止する
- [ ] ffmpeg で WebM → MP4 変換が成功する

### 2-4. ダウンロード・削除
- [ ] 完了した動画のダウンロードボタンが機能する（※要手動テスト）
- [ ] 削除ボタンで動画が削除される
- [ ] エラー状態の動画も削除できる

---

## 3. スマホ縦画面対応

### 3-1. プレビューエンジン
- [x] `?orientation=portrait` で canvas が 720×1280+ になる（2026-03-22 Playwright: 720x1558）
- [x] 横画面では従来通り 1280×720（2026-03-22 Playwright）
- [ ] `?mode=play` で自動検出（innerWidth < innerHeight なら縦）

### 3-2. UI 配置（アンカー）
- [x] メッセージウィンドウが画面下部に配置（2026-03-22 Playwright スクリーンショット確認）
- [x] メニューバーが画面上部に配置（2026-03-22 スクリーンショット確認）
- [ ] 名前ボックスがメッセージの上に配置
- [ ] デバイス高さが変わっても崩れない

### 3-3. 各画面
- [x] タイトル画面が縦長で表示される（2026-03-22 Playwright スクリーンショット確認）
- [x] テキスト表示が正常（2026-03-22 Playwright スクリーンショット確認）
- [x] 選択肢が画面中央に表示、暗幕が全画面カバー（2026-03-22 Playwright スクリーンショット確認）
- [ ] 背景が cover スケーリング
- [ ] キャラが画面下端に足を揃えて表示
- [ ] END 画面が縦長で表示される

### 3-4. エディタ連携
- [x] レイアウトプリセットに「スマホ縦画面」がある（2026-03-22 Playwright）
- [x] 選択 → 保存 → リロードで `playLayout.screenMode=portrait` が保持される（2026-03-22 Playwright）
- [x] プレビュー iframe に `orientation=portrait` が付く（2026-03-22 Playwright）
- [x] 実行ボタンの URL に `orientation=portrait` が付く（2026-03-22 Playwright）
- [ ] レイアウトプレビューキャンバスが縦長になる

---

## 4. トークン期限切れダイアログ

### 4-1. Next.js
- [x] 期限切れトークンで API 呼び出し時にダイアログが表示される（2026-03-22 Playwright）
- [ ] 「OK」でログイン画面に遷移する
- [ ] 「キャンセル」で閉じる
- [ ] ログイン/登録 API の 401 では表示されない
- [ ] 連続 401 でダイアログが複数表示されない

### 4-2. エディタ
- [x] 期限切れトークンで保存/プレビュー時にダイアログが表示される（2026-03-22 Playwright）
- [ ] 「OK」でログイン画面に遷移する

---

## 5. エディタ UI 改善

### 5-1. キャラブロック
- [x] キャラ選択ドロップダウンで切替できる（2026-03-22 Playwright: cc 選択肢確認）
- [ ] 表情選択ドロップダウンで切替できる（※要手動テスト）
- [ ] ✏️ ボタンで編集モーダルが開く
- [ ] + ボタンで新規キャラ作成できる
- [ ] 保存→リロードで設定が保持される

### 5-2. レイアウトエディタ
- [x] プリセット切替でプレビューが変わる（2026-03-22 Playwright）
- [ ] ドラッグで要素移動ができる（※要手動テスト）
- [ ] プレビューサイズがプリセットの resolution に連動する

### 5-3. 実行ボタン
- [x] `mode=play` でタイトル画面が表示される（2026-03-22 Playwright）
- [ ] 縦画面プリセット選択時にウィンドウサイズが 400×800

---

## 6. playLayout の DB 保存

- [x] エディタでレイアウト保存 → ヘッダー💾保存で DB に入る（2026-03-22 Playwright）
- [x] リロード後に playLayout が復元される（2026-03-22 Playwright: iframe src 確認）
- [x] EditorPage.tsx でプロジェクト読み込み時に playLayout が含まれる（2026-03-22 コード確認）
- [x] Header.tsx の保存処理に playLayout が含まれる（2026-03-22 コード確認）

---

## テスト結果サマリー

| カテゴリ | 自動テスト済み | 手動テスト要 | 統合テスト要 |
|---------|-------------|------------|------------|
| タイトル画像 | 3 | 15 | 3 |
| 動画クリップ | 3 | 4 | 11 |
| 縦画面対応 | 10 | 4 | 0 |
| トークン期限切れ | 2 | 4 | 0 |
| エディタ UI | 3 | 5 | 0 |
| playLayout 保存 | 4 | 0 | 0 |
| **合計** | **25** | **28** | **14** |

---

## テスト実行方法

```bash
# API ユニットテスト
npm test -w @kaedevn/hono

# Web エンジンテスト
npm test -w @kaedevn/web

# Playwright E2E
npx playwright test tests/shared/flow/

# 型チェック
npm run typecheck
```

## 手動テスト URL

```
# プロジェクトページ（タイトル画像・動画）
http://localhost:3000/projects/<projectId>

# サムネイル管理
http://localhost:3000/projects/<projectId>/thumbnails

# エディタ
http://localhost:5176/projects/editor/<projectId>?userId=<userId>&token=<token>

# 縦画面プレビュー
http://localhost:5175/ksc-demo.html?work=<projectId>&mode=play&orientation=portrait

# 横画面プレビュー
http://localhost:5175/ksc-demo.html?work=<projectId>&mode=play
```
