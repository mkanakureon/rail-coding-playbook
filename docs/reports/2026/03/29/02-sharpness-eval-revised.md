# kaedevn 尖り設計評価（修正版）— 「今の A」は何か

> Generated by Claude Opus 4.6

## 背景

前回評価（01-sharpness-eval-cccca.md）では Switch を A と仮定して評価した。
ユーザーから戦略の補正あり:

> Switch は最終目標。先にリポジトリ全体の完成度を上げる・集客する・お金を稼ぐ。その収益と実績で Switch を完成させる。

つまり **「今の A」は Web で勝負できる一点** でなければならない。

## A 候補の棚卸し

| 候補 | 完成度 | 競合との差 |
|------|--------|-----------|
| エディタ（22種ブロック + タイムライン + Live2D + カメラ） | 高い | 機能は多いが「これがあるから kaedevn」が見えにくい |
| 公式アセット（DB 3層 + セマンティック検索 + 自動セットアップ） | 8.5/10 | 「開いた瞬間に素材が揃っている」は他にない |
| 4PF 完全互換（Op IR → Web/macOS/Android/Switch） | Web 95%, Native 75% | 今は Web だけで十分。Switch が動いて初めて訴求力 |
| CLI 完備（32スキル + Claude Code でゲーム作成可能） | 完成 | 他にない |
| GUI ⇔ KSC 双方向変換 | 完成 | ティラノにない |

## 判定

### A =「素材ゼロ・スキルゼロの人が、AI と一緒に 5 分でビジュアルノベルを完成させられる」

分解すると:

- **公式アセット** → 素材ゼロを解決
- **CLI 完備** → Claude Code でプロジェクト作成〜シナリオ編集〜プレビューまで一気通貫
- **エディタ** → 結果を目で見て調整できる
- **4PF 互換** → 将来 Switch に出せる（アップセル）

### 競合比較

| | kaedevn | ティラノビルダー | Ren'Py | NovelAI |
|---|---------|--------------|--------|---------|
| 素材ゼロで始められる | **公式アセット** | 自分で用意 | 自分で用意 | 画像生成のみ |
| CLI 完備・Claude Code で作成可能 | **32スキル + CLI + _ai_context API** | なし | なし | なし |
| GUI で調整 | **22種ブロック** | あり | なし（コード） | なし |
| Switch に出せる | 将来 | 不可 | 不可 | 不可 |

**「CLI 完備 × 公式アセット × GUI」の三位一体が、他のどのツールにもない。**

### CLI 完備の実態

32 スキル + 専用 CLI スクリプト群で、Claude Code のターミナルだけでゲーム制作が完結する:

| ステップ | スキル/CLI | 状態 |
|---------|-----------|------|
| プロジェクト作成 | `/create-project`（7ジャンルテンプレート + テキスト台本投入） | 完成 |
| キャラクター追加 | `/character`（公式アセットから作成・表情管理） | 完成 |
| シナリオ編集 | `/edit-blocks`（ブロック CRUD・ページ管理・検証） | 完成 |
| アセット管理 | `/asset`（アップロード・公式連携・マイアセット） | 完成 |
| マップ作成 | `/map`（タイル配置・衝突判定・PNG プレビュー） | 完成 |
| プレビュー | API `/api/preview/:id` + Web エンジン | 完成 |
| テスト | `/test`（変更関連テスト自動選択） | 完成 |
| デプロイ | `/deploy-azure` + `git push`（GitHub Actions 自動） | 完成 |

## 透明化リスク（修正版）

前回「AI ドキュメント 371 件は無駄」と評価したが、A が「CLI + Claude Code でゲームを作れる」なら CLI・スキル・API の設計に時間を使うのは正しい。

| やっていること | A への貢献 | 判定 |
|-------------|-----------|------|
| CLI・スキル・_ai_context ��計 | 直接 | OK |
| 公式アセット管理 | 直接 | OK |
| エディタ機能拡充 | 直接 | OK |
| RPG Studio / マップエディタ | A を薄める | 保留推奨 |
| OSS 戦略文書 50+ 件 | 集客に間接貢献 | 量を減らす |
| チームスケーリング文書 | 今ではない | 不要 |
| BigQuery ロギング | 集客の計測 | OK |

**RPG Studio が最大の透明化リスク。** ノベル × CLI の A が固まる前にジャンルを広げると BBBBB になる。

## 推奨アクション

1. **A を言語化する**: 「素材ゼロ・スキルゼロで 5 分でゲームが動く」をランディングページの 1 行目に
2. **5 分デモを作る**: Claude Code で `/create-project` → `/edit-blocks` → プレビュー → 動くゲームが出る、の動画 1 本
3. **RPG Studio を凍結**: A が証明されるまで
4. **公式アセットを増やす**: 「ファンタジー」以外に「学園」「SF」「和風」プリセット追加 → 「どんなジャンルでも 5 分」
5. **CLI フローを磨く**: `/create-project` → `/character` → `/edit-blocks` → プレビューの一気通貫を迷わないレベルに
