# 画面 LAYOUT リファレンス

> Generated by Claude Opus 4.6

tsx ファイルと 1:1 で対応する TARGET / LAYOUT の一覧。
画面改修時にこのファイルを参照し、LAYOUT ブロック名で位置を特定する。

**更新ルール**: tsx の UI 構成が変わったら、このファイルの該当エントリも更新する。

---

## Next.js — Public Pages

### / (トップページ)

```
TARGET: / — apps/next/app/page.tsx
LAYOUT:
- HeroBanner: ロゴ "kaedevn" + BETA バッジ + キャッチコピー + CTA ボタン（マイページへ / 新規登録 / ログイン）
- BelowHeroAd: 広告枠
- FeaturesSection: 3カラム（直感的なエディタ / すぐに公開 / マルチプラットフォーム）
- HowToUseSection: 3ステップ（アカウント登録 / エディタで作成 / ワンクリック公開）
- WorksSection: 「最新の作品」見出し + 最大8件カード + 「もっと見る」リンク
- CTASection: 未ログイン時のみ表示、グラデーション CTA ボックス
- RoadmapSection: 縦タイムライン（Phase 1〜3）
- StickyBottomAd: 広告枠
STATES: loading（スケルトン4枚）/ empty / default
```

### /login

```
TARGET: /login — apps/next/app/(public)/login/page.tsx
LAYOUT:
- H1: ログイン
- Form: メールアドレス + パスワード + ログインボタン
- RegisterLink: 新規登録リンク
- InContentAd: 広告枠
STATES: success バナー（registered=true）/ error バナー / loading スピナー
```

### /register

```
TARGET: /register — apps/next/app/(public)/register/page.tsx
LAYOUT:
- H1: 新規登録
- Form: ユーザー名（バリデーションヒント付き）+ メールアドレス + パスワード（ヒント付き）+ 登録ボタン
- LoginLink: ログインリンク
STATES: error バナー / loading スピナー
```

### /works

```
TARGET: /works — apps/next/app/(public)/works/page.tsx
LAYOUT:
- Header: H1 "作品一覧" + マイページリンク + ソートタブ（新着順 / 人気順）+ カテゴリフィルター pill（すべて + カテゴリ一覧）
- Main: WorkCard グリッド（4カラム、サムネイル / タイトル / 説明 / タグ / 作者 / 再生数 / いいね数）+ InListAd
- Pagination: 前へ / ページ N / 次へ
- StickyBottomAd: 広告枠
STATES: error バナー / loading スピナー / empty（作品がありません）/ default
```

### /play/:id

```
TARGET: /play/:id — apps/next/app/(public)/play/[id]/page.tsx
LAYOUT (プレイ前):
- FullscreenOverlay: グラデーション背景 + タイトル（h1）+ 説明 + カテゴリ/タグバッジ + 作者リンク + 再生数/いいね数 + LikeButton + START ボタン + 戻るボタン + InContentAd
LAYOUT (プレイ中):
- GameContainer: フルスクリーン iframe（ksc-demo.html）
- ExitButton: ✕（右上）
- PortraitWarning: 横向き推奨オーバーレイ
STATES: loading（全画面スピナー）/ error（全画面エラー）/ pre-play / playing
```

### /users/:id

```
TARGET: /users/:id — apps/next/app/(public)/users/[id]/page.tsx
LAYOUT:
- ProfileHeader: アバター（頭文字）+ ユーザー名 + 作品数 + 登録日
- BelowHeroAd: 広告枠
- Main: H2 "公開作品" + カードグリッド（サムネイル / タイトル / 説明 / カテゴリ/タグ / 再生数/いいね数 / 日付）
- Pagination: 前へ / ページ X/Y / 次へ
- StickyBottomAd: 広告枠
STATES: error（全画面エラー + 戻るリンク）/ loading / empty / default
```

### /about

```
TARGET: /about — apps/next/app/(public)/about/page.tsx
LAYOUT:
- H1: サービスについて
- Section: kaedevnとは（説明文）
- Section: 特徴（4項目リスト）
- Section: 運営（個人運営の旨 + お問い合わせリンク）
```

### /help

```
TARGET: /help — apps/next/app/(public)/help/page.tsx
LAYOUT:
- H1: ヘルプ / よくある質問と使い方ガイド
- Section: はじめに（2件 details FAQ）
- Section: エディタの使い方（3件 details FAQ）
- Section: トラブルシューティング（2件 details FAQ）
```

### /contact

```
TARGET: /contact — apps/next/app/(public)/contact/page.tsx
LAYOUT:
- H1: お問い合わせ
- SupportNotice: 個人運営の注意書き（amber）
- Form: 名前 + メールアドレス + 種別 select（不具合/要望/アカウント/その他）+ 内容 textarea（max 5000）+ 送信ボタン
STATES: sent（✅ 成功パネル + トップへ戻る）/ error バナー / sending スピナー
```

### /terms

```
TARGET: /terms — apps/next/app/(public)/terms/page.tsx
LAYOUT:
- H1: 利用規約 + 更新日
- Section: 第1条〜第6条（適用 / アカウント / 禁止事項 / コンテンツ / 免責事項 / 規約の変更）
- InContentAd: 広告枠（第3条と第4条の間）
```

### /privacy

```
TARGET: /privacy — apps/next/app/(public)/privacy/page.tsx
LAYOUT:
- H1: プライバシーポリシー + 更新日
- Section: 収集する情報 / 情報の利用目的 / 第三者への提供
- InContentAd: 広告枠
- Section: Cookieの使用 / データの保護 / お問い合わせリンク
```

### /docs

```
TARGET: /docs — apps/next/app/(public)/docs/page.tsx
LAYOUT:
- H1: 仕様書・設計書
- DocList: 5件リンクカード（KSスクリプト / JSON中間表現 / エンジン概要 / KNFインタプリタ / タイムライン演出）
```

### /docs/:slug

```
TARGET: /docs/:slug — apps/next/app/(public)/docs/[slug]/page.tsx
LAYOUT:
- BackLink: ← 仕様書一覧
- ArticleTitle: ドキュメントタイトル
- ProseContent: 技術仕様本文（テーブル / コードブロック / 見出し等）
STATES: notFound（slug が存在しない場合）
```

---

## Next.js — Private Pages

### /mypage

```
TARGET: /mypage — apps/next/app/(private)/mypage/page.tsx
LAYOUT:
- HeaderBanner: グラデーション + アバター頭文字 + ユーザー名 + メール + ボタン（管理画面 if admin / メッセージ（未読バッジ）/ ログアウト）
- StatsRow: プロジェクト数 / 総プレイ数 / いいね数
- BelowHeroAd: 広告枠
- TabNavigation: sticky — プロジェクト / プロフィール
- ProjectsTab: 見出し + 新規作成ボタン + プロジェクトカード一覧（タイトル / 更新日 / 公開中バッジ）
- ProfileTab: アバター + ユーザー名/メール/ロール カード + フィールド（ID / ユーザー名 / メール / 権限）+ リンク（作品を見る / メッセージ）
- InContentAd: 広告枠
- CreateModal: タイトル入力 + 作成/キャンセルボタン
STATES: loading（スケルトン3行）/ empty / default
```

### /mypage/messages

```
TARGET: /mypage/messages — apps/next/app/(private)/mypage/messages/page.tsx
LAYOUT:
- BackLink: ← マイページ
- H1: メッセージ
- TotalCount: N件
- MessageList: 行リンク（未読ドット + 件名 + 日付 + 本文プレビュー1行）
- Pagination: 前へ / X/Y / 次へ
STATES: loading / empty（メッセージはありません）/ default
```

### /mypage/messages/:id

```
TARGET: /mypage/messages/:id — apps/next/app/(private)/mypage/messages/[id]/page.tsx
LAYOUT:
- BackLink: ← メッセージ一覧
- MessageCard: 件名（h1）+ 日付 / 「運営からのメッセージ」+ 本文
STATES: loading / not found（赤テキスト）/ default
```

### /projects

```
TARGET: /projects — apps/next/app/(private)/projects/page.tsx
LAYOUT:
- RedirectNotice: 「リダイレクト中...」
STATES: 即座に /mypage へリダイレクト
```

### /projects/:id

```
TARGET: /projects/:id — apps/next/app/(private)/projects/[id]/page.tsx
LAYOUT:
- BackLink: ← プロジェクト一覧
- TitleSection: タイトル表示/編集モード（入力 + 保存/キャンセル or タイトル + 編集ボタン）
- Metadata: 作成日 / 更新日（2カラム）
- Actions: エディタで編集ボタン（外部リンク）/ 作品を公開ボタン / プレビューボタン
- DangerZone: プロジェクトを削除ボタン（赤）
- PublishDialog: タイトル / 説明 / サムネURL / カテゴリ / タグ入力フォーム
- DeleteDialog: 削除確認ダイアログ
STATES: loading / error（エラーパネル + 戻るリンク）/ default
```

### /my-assets

```
TARGET: /my-assets — apps/next/app/(private)/my-assets/page.tsx
LAYOUT:
- H1: アセット管理
- TabBar: 公式アセット / マイアセット
- KindFilter: pill — すべて / 画像 / 音声 / キャラクラス
- CategoryFilter: pill（種別選択時に表示）— 背景/キャラクター/演出/UI（画像）; BGM/SE/ボイス（音声）
- SubcategoryFilter: pill（API から動的）
- UploadButton: ファイル入力（マイタブ + kind!=ch-class のみ）
- AssetGrid: カード — サムネイル/アイコン + displayName + kind/category + プロジェクトに追加 select + 削除ボタン（マイタブのみ）
STATES: loading / empty（タブ別）/ uploading / importing
```

### /preview

```
TARGET: /preview — apps/next/app/(private)/preview/page.tsx
LAYOUT (プレイ前):
- FullscreenOverlay: グラデーション + 👁️ プレビューモード見出し + ブロック数 + START / 閉じるボタン
LAYOUT (プレイ中):
- PreviewBadge: 👁️ プレビュー（左上）
- GameCanvas: 背景画像 + キャラクタースプライト（左/中央/右）
- TextWindow: キャラ名 + セリフ + 選択肢ボタン + 次へボタン
- ControlBar: 最初からボタン / 閉じるボタン（右上）
STATES: error（全画面エラー + 閉じる）/ pre-play / playing
```

---

## Next.js — Admin Pages

### /admin

```
TARGET: /admin — apps/next/app/(private)/admin/page.tsx
LAYOUT:
- H1: ダッシュボード
- StatsGrid: 4カード — ユーザー数（blue）/ プロジェクト数（green）/ 公開作品数（purple）/ アセット数（orange）
STATES: loading / default
```

### /admin/users

```
TARGET: /admin/users — apps/next/app/(private)/admin/users/page.tsx
LAYOUT:
- H1: ユーザー管理
- SearchAndFilter: テキスト検索 + 検索ボタン + ステータスフィルタ select（全/有効/停止中）
- TotalCount: 全N件
- UsersTable: ユーザー名（リンク）/ メール / ロール（バッジ）/ ステータス（バッジ）/ PJ数 / 登録日 / 操作（停止/復帰ボタン）
- Pagination: 前へ / X/Y / 次へ
STATES: loading 行 / empty 行 / default
```

### /admin/users/:id

```
TARGET: /admin/users/:id — apps/next/app/(private)/admin/users/[id]/page.tsx
LAYOUT:
- BackLink: ← ユーザー一覧 + H1: ユーザー名
- TwoColGrid:
  - UserInfoCard: ID / メール / メール認証 / ロール（バッジ）/ ステータス（バッジ）/ 登録日
  - StatsCard: プロジェクト数 / 公開作品数
- Actions: アカウント停止/復帰ボタン + メッセージ送信ボタン
- MessageDialog: 送信先表示 + 件名 + 本文フォーム
STATES: loading / not found / default
```

### /admin/assets

```
TARGET: /admin/assets — apps/next/app/(private)/admin/assets/page.tsx
LAYOUT:
- H1: アセット管理 + 一括アップロードボタン
- KindTabs: すべて / 背景 / キャラクター / BGM / SE / ボイス
- CategoryFilter: select（API から動的）
- TotalCount: N件
- AssetGrid: カード — サムネイル（画像 or アイコン）+ name + kind バッジ + category バッジ + date
- Pagination: 前へ / X/Y / 次へ
- LightboxModal: フル画像プレビューオーバーレイ
- BulkUploadModal: フォルダ構成ヒント + 種別 select + フォルダ選択 + カテゴリ一覧 + プログレスバー
STATES: loading / empty / default; lightbox; bulk upload + progress
```

### /admin/official-assets

```
TARGET: /admin/official-assets — apps/next/app/(private)/admin/official-assets/page.tsx
LAYOUT:
- H1: 公式アセット管理 + アップロードボタン
- UploadPanel: 種別 select + カテゴリ select + ファイル input（複数）+ アップロードボタン
- KindTabs: すべて / 背景 / キャラクター / BGM
- CategoryFilter: pill（すべて / basic / ファンタジー/屋外 / 現代/屋外 / 現代/屋内 / 自然）
- TotalCount: N件
- AssetGrid: サムネイル or 🎵 アイコン + name + kind/category バッジ + date + 削除ボタン
- Pagination: 前へ / X/Y / 次へ
- LightboxModal: フル画像オーバーレイ
STATES: loading / empty / default
```

### /admin/messages

```
TARGET: /admin/messages — apps/next/app/(private)/admin/messages/page.tsx
LAYOUT:
- H1: メッセージ管理 + 新規送信ボタン
- TotalCount: 送信済みN件
- MessagesTable: 送信先 / 件名 / 既読（バッジ）/ 送信日時
- Pagination: 前へ / X/Y / 次へ
- SendDialog: 送信先ユーザーID + 件名 + 本文フォーム
STATES: loading 行 / empty 行 / default
```

### /admin/query

```
TARGET: /admin/query — apps/next/app/(private)/admin/query/page.tsx
LAYOUT:
- H1: SQLクエリ
- QuickTableButtons: users / projects / works / assets / messages pill
- SQLInput: textarea（4行、monospace）+ ヒント「Cmd+Enter で実行 / SELECTのみ」+ 実行ボタン
- ErrorBanner: 赤 monospace エラー
- ResultsSection: 行数 + 実行時間 + スクロール可能データテーブル（sticky ヘッダー + 行番号 + データ列）
STATES: loading（ボタン disabled）/ error / empty 結果 / 結果テーブル（max 500行）
```

### /admin/backups

```
TARGET: /admin/backups — apps/next/app/(private)/admin/backups/page.tsx
LAYOUT:
- H1: バックアップ管理 + 更新ボタン + メール通知テストボタン
- NotifyResult: success/error バナー
- ErrorBanner: yellow 警告
- SummaryCards: ダンプ数 / 最終バックアップ / 最古のダンプ（3カラム）
- AutoBackupInfo: Azure 保持期間 / geo 冗長（2カラム）
- DumpListTable: ファイル名 / サイズ / 日時
- ChecklistCard: 4項目チェック（dumps exist / last <48h / Azure retention / size > 0）
STATES: loading / error / default
```

### /admin/settings

```
TARGET: /admin/settings — apps/next/app/(private)/admin/settings/page.tsx
LAYOUT:
- H1: 管理設定
- StorageSection: 2択ラジオカード — ローカルストレージ / Azure Blob Storage（未設定時 disabled）+ 現在モード表示
- AssetNormalizationSection: 説明テキスト + 「アセットURLを正規化」ボタン（orange）
STATES: loading / default
```

### /admin/works, /admin/announcements, /admin/reports, /admin/reports-abuse, /admin/audit-log, /admin/storage

```
TARGET: /admin/works — apps/next/app/(private)/admin/works/page.tsx
TARGET: /admin/announcements — apps/next/app/(private)/admin/announcements/page.tsx
TARGET: /admin/reports — apps/next/app/(private)/admin/reports/page.tsx
TARGET: /admin/reports-abuse — apps/next/app/(private)/admin/reports-abuse/page.tsx
TARGET: /admin/audit-log — apps/next/app/(private)/admin/audit-log/page.tsx
TARGET: /admin/storage — apps/next/app/(private)/admin/storage/page.tsx
LAYOUT:
- H1: ページタイトル
- PlaceholderCard: 準備中
```

---

## Editor App

### /projects/editor/:workId (EditorPage)

```
TARGET: /projects/editor/:workId — apps/editor/src/pages/EditorPage.tsx
LAYOUT (デスクトップ ≥1024px):
- Header: userId + workId + 保存ボタン + タイムライントグルボタン
- LeftSidebar: SidebarOutline（シーン/ページ一覧、ブロック選択）
- ResizeHandle: 左右リサイズ
- Center: BlockList（メインブロック編集キャンバス）
- TimelineResizeHandle: 上下リサイズ（条件付き）
- TimelinePanel: アニメーションタイムライン（条件付き、120〜500px）
- RightResizeHandle: 左右リサイズ
- RightSidebar: SidebarInspector（選択ブロックのプロパティ）
- PageNavigator: sticky 下部ページナビゲーション
- ToastContainer: トースト通知
LAYOUT (モバイル <1024px):
- Header: 自動非表示ヘッダー
- TabNavigation: エディタ / キャラクター / アセット / 設定
- ActiveTab: BlockList or CharacterPanel or AssetPanel or SettingsPanel
- PageNavigator: 下部ページナビゲーション
- ToastContainer: トースト通知
STATES: loading（Loading… + 戻るリンク）/ auth redirect / default
```

### EditorPage — SettingsPanel

```
TARGET: /projects/editor/:workId (SettingsPanel — 設定タブ) — apps/editor/src/components/panels/SettingsPanel.tsx
LAYOUT:
- SectionHeader: h2 "作品設定"
- TitleInput: ラベル "作品タイトル" + テキスト入力（blur で保存）+ ヘルパーテキスト
- ProjectInfo: ラベル "プロジェクト情報" + 情報カード（プロジェクトID / ページ数 / アセット数 / 作成日 / 更新日）
```

### EditorPage — CharacterPanel

```
TARGET: /projects/editor/:workId (CharacterPanel — キャラクタータブ) — apps/editor/src/components/panels/CharacterPanel.tsx
LAYOUT:
- Header: h2 "キャラクター管理" + "+ 新しいキャラクター" ボタン
- CharacterGrid: カードグリッド — サムネイル（デフォルト表情 or プレースホルダー）+ キャラ名 + slug + 表情数 + 編集/削除ボタン + マイキャラクター登録ボタン
- CharacterEditModal: slug / 表示名 / 表情差分一覧（サムネイル + slug + 名前 + アセット選択 + 削除）/ デフォルト表情 select
- DeleteConfirmSheet: 削除確認シート
STATES: loading / empty（CTA 付き）/ default
```

### EditorPage — AssetPanel

```
TARGET: /projects/editor/:workId (AssetPanel — アセットタブ) — apps/editor/src/components/panels/AssetPanel.tsx
LAYOUT:
- Header: h2 "マイアセット管理" + アップロードボタン（🖼️ 背景を追加 / 🎵 BGMを追加）
- FilterButtons: 全部(N) / 🖼️ 背景(N) / 👤 キャラ(N) / 🎵 BGM(N)
- UploadProgress: アップロード中…（条件付き）
- AssetGrid: カード — 削除ボタン(×) + サムネイル(h-24) + アセット名 + SlugEditor（インライン編集）
- EmptyState: フィルター別メッセージ
- DeleteConfirmSheet: 削除確認シート
STATES: uploading / empty（フィルター別）/ default
```

### EditorPage — TimelinePanel

```
TARGET: /projects/editor/:workId (TimelinePanel — 下部パネル) — apps/editor/src/components/panels/TimelinePanel.tsx
LAYOUT:
- PlaybackControls: 再生/停止 + 時間カウンター
- TrackList: トラック一覧（クリップ + キーフレーム）
- PropertyEditor: チャンネルプロパティ（x, y, opacity, scale, rotation）
- KeyframeEditor: キーフレーム編集UI + イージングドロップダウン
- SeekBar: シークバー / スクラバー
```

---

## Editor App — 未ルーティングページ

以下は `apps/editor/src/pages/` に存在するが、`App.tsx` でルーティングされていないページ。

### LoginPage (未ルーティング)

```
TARGET: (未ルーティング) — apps/editor/src/pages/LoginPage.tsx
LAYOUT:
- Header: h1 "KAEDEVN Editor" + 「ログインしてください」
- UserCardGrid: ユーザーカード一覧（アバター頭文字 + ユーザー名 + @userId）
STATES: loading / default / already logged in → リダイレクト
```

### UserSelectPage (未ルーティング)

```
TARGET: (未ルーティング) — apps/editor/src/pages/UserSelectPage.tsx
LAYOUT:
- Header: h1 "KAEDEVN Editor" + 「ユーザーを選択してください」
- UserCardGrid: ユーザーカード + ➕ 新規ユーザーカード（10人未満の場合）
- LimitWarning: yellow バナー（10人以上の場合）
- NewUserDialog: ユーザーID + 表示名 + エラー + 作成/キャンセル
STATES: loading / <10 users / ≥10 users / dialog
```

### UserProjectListPage (未ルーティング)

```
TARGET: (未ルーティング) — apps/editor/src/pages/UserProjectListPage.tsx
LAYOUT:
- Header: 🏠 ユーザー選択 戻る + h1 "{name} のプロジェクト" + プロジェクト数 + 新規作成ボタン
- ProjectGrid: カード（サムネイル + タイトル + ページ数 + 更新日）
- EmptyState: 「プロジェクトがありません」+ CTA
STATES: loading / empty / creating / default
```

### WorksPage, FavoritesPage, GuidePage (未ルーティング — プレースホルダー)

```
TARGET: (未ルーティング) — apps/editor/src/pages/WorksPage.tsx
TARGET: (未ルーティング) — apps/editor/src/pages/FavoritesPage.tsx
TARGET: (未ルーティング) — apps/editor/src/pages/GuidePage.tsx
LAYOUT:
- BackLink: ← マイページに戻る
- PlaceholderCard: タイトル + 「準備中」テキスト
```

### UserSettingsPage (未ルーティング)

```
TARGET: (未ルーティング) — apps/editor/src/pages/UserSettingsPage.tsx
LAYOUT:
- Header: ← 戻る + h1 "ユーザー設定"
- UserInfoCard: アバター + ユーザー名 + @userId
- AccountCard: h2 "アカウント" + ログアウトボタン（赤）+ 説明テキスト
STATES: no user → /login リダイレクト / default
```
