---
generated_by: Claude Opus 4.6
date: 2026-04-01
type: report
project: kaedevn-monorepo
---

# 投稿動画作成 API — テストギャップ分析・テスト追加レポート

> Generated by Claude Opus 4.6 — 2026年4月1日

## 背景

Videos API（`apps/hono/src/routes/videos.ts` 201行）に対し、既存テスト（`videos-api.test.ts` 50行）は認証チェックの3テストのみ。動画生成のコアロジックが未検証のため、テストを追加した。

## API 仕様

| エンドポイント | 認証 | 処理 |
|---------------|------|------|
| `POST /api/works/:id/video` | 要（作者のみ） | DB に `pending` レコード作成 → バックグラウンドで generate-clip.ts 実行 → done/error に遷移 |
| `GET /api/works/:id/video` | 不要 | 作品の動画一覧を返却 |
| `DELETE /api/works/:id/video/:videoId` | 要（作者のみ） | ファイル削除 + DB レコード削除 |

**ビジネスロジック:**
- Duration: 5〜120秒（デフォルト30秒）
- アスペクト比: 9:16（720×1280）/ 16:9（1280×720）
- 処理中の動画がある場合は 409 で拒否
- 生成: Playwright ヘッドレス録画 → ffmpeg で WebM→MP4 変換

## テストギャップ（追加前）

| 項目 | 重要度 | 既存テスト |
|------|--------|-----------|
| POST/DELETE 認証なし → 401 | 高 | ✅ あり |
| GET 認証不要 | 高 | ✅ あり |
| POST 正常系（202、レスポンス構造） | **高** | ❌ なし |
| ステータス遷移（pending → processing → done/error） | **高** | ❌ なし |
| 処理中の重複リクエスト → 409 | **高** | ❌ なし |
| Duration バリデーション（クランプ 5〜120） | 中 | ❌ なし |
| アスペクト比による width/height 計算 | 中 | ❌ なし |
| DELETE 正常系 | 中 | ❌ なし |
| 他人の作品 → 403 | 中 | ❌ なし |
| 存在しない作品 → 404 | 中 | ❌ なし |

## 追加したテスト

既存テストは DB 不要の認証チェックのため、同じパターンで DB モック（Prisma モック）を使った正常系・異常系テストを追加。

| テスト | 検証内容 |
|-------|---------|
| POST 正常系 202 | videoId・status・format・width・height のレスポンス構造 |
| POST 9:16 デフォルト | 720×1280 の解像度計算 |
| POST 16:9 指定 | 1280×720 の解像度計算 |
| POST Duration クランプ | 5未満→5、120超→120 |
| POST 処理中 409 | pending/processing 状態で重複拒否 |
| POST 存在しない作品 404 | resolveWork が null |
| POST 他人の作品 403 | userId 不一致 |
| DELETE 正常系 | DB + ファイル削除の成功 |
| DELETE 存在しない動画 404 | videoId 不一致 |
| DELETE 他人の作品 403 | userId 不一致 |
| GET 空一覧 | 動画なしで空配列 |

---

> この文書は Claude Opus 4.6 が生成しました。内容の正確性は人間のレビューを経て確認してください。
>
> Claude Opus 4.6 — Anthropic
