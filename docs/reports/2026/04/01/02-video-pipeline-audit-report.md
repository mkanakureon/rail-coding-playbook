---
generated_by: Claude Opus 4.6
date: 2026-04-01
type: report
project: kaedevn-monorepo
---

# ノベルゲーム投稿動画作成機能 — 調査・テスト監査レポート

> Generated by Claude Opus 4.6 — 2026年4月1日

## 背景

Gemini CLI が生成したテスト分析文書（`docs/10_ai_docs/2026/04/01/`）の妥当性検証の一環として、動画作成・投稿パイプラインの実装とテストカバレッジを調査した。

## パイプライン構成

```
Editor UI → API(/api/works/:id/video) → generate-clip.ts(Playwright+ffmpeg)
                                            ↓
OBS録画(obs-stream.mjs) → recordings/   → youtube-upload.mjs → YouTube
```

## 主要ファイル一覧

| レイヤー | ファイル | 行数 | 役割 |
|---------|---------|------|------|
| **API** | `apps/hono/src/routes/videos.ts` | 201 | 動画生成・一覧・削除 REST API |
| **生成** | `scripts/cli/video/generate-clip.ts` | 179 | Playwright録画 → ffmpeg で WebM→MP4 変換 |
| **録画** | `scripts/stream/record-demo.ts` | 157 | エディタ操作のデモ録画 |
| **OBS** | `scripts/stream/obs-stream.mjs` | 284 | OBS WebSocket 制御（配信・録画・テキストOVL） |
| **投稿** | `scripts/stream/youtube-upload.mjs` | 368 | YouTube Data API v3、OAuth 2.0、アップロード |
| **自動化** | `scripts/stream/auto-record.sh` | 52 | dev起動→録画→クリーンアップ |

### API エンドポイント（videos.ts）

| Method | Path | 認証 | 説明 |
|--------|------|------|------|
| `POST` | `/api/works/:id/video` | 要（作者のみ） | 動画生成開始。202 返却、バックグラウンドで generate-clip.ts 実行 |
| `GET` | `/api/works/:id/video` | 不要 | 作品の動画一覧取得 |
| `DELETE` | `/api/works/:id/video/:videoId` | 要（作者のみ） | 動画削除（ファイル + DBレコード） |

ステータス遷移: `pending → processing → done / error`

### generate-clip.ts の仕様

- Playwright ヘッドレスブラウザで `ksc-demo.html` を録画
- 解像度: 9:16（720×1280）または 16:9（1280×720）
- WebM → MP4 変換（ffmpeg）
- 完了シグナル: `page.title() === '__CLIP_DONE__'`

### youtube-upload.mjs の仕様

- OAuth 2.0 認証フロー（ローカルコールバック port 8889）
- トークン永続化: `.youtube-token.json`
- アップロード: 進捗バー表示、カテゴリ固定（Science & Technology）
- コマンド: `auth`（初回認証）、`upload <video>`（投稿）、`status <videoId>`（処理状況確認）

## テスト状況

### 既存テスト

| カテゴリ | テストファイル | 行数 | 内容 |
|---------|--------------|------|------|
| **API テスト** | `apps/hono/test/videos-api.test.ts` | 50 | POST/DELETE の認証チェックのみ |
| **録画デモ** | `tests/local/recording/` (11ファイル) | 3,179 | エディタ操作のショーケース録画 |

### 録画テスト一覧（tests/local/recording/）

| ファイル | 行数 | 目的 |
|---------|------|------|
| `record-demo.spec.ts` | 88 | 基本操作デモ |
| `bg-showcase.spec.ts` | 312 | 背景アセット10種ショーケース |
| `block-showcase.spec.ts` | 241 | 全ブロック種デモ |
| `block-coverage-showcase.spec.ts` | 162 | 14ブロック種カバレッジ |
| `camera-operations.spec.ts` | 384 | カメラ操作（zoom/pan/shake） |
| `create-add-blocks.spec.ts` | 456 | プロジェクト作成→ブロック追加→再生 |
| `edit-character-properties.spec.ts` | 319 | キャラプロパティ編集 |
| `effect-showcase.spec.ts` | 169 | 8種エフェクトデモ |
| `press-demo.spec.ts` | 439 | プレス向けフルデモ |
| `press-preview-only.spec.ts` | 102 | プレビューのみデモ |
| `timeline-operations.spec.ts` | 507 | タイムライン操作 |

### 録画テストの特徴

- **目的はデモ動画生成** — アサーション少なめ、動画出力が主目的
- **OBS 連携はフェイルセーフ** — OBS 未起動でもテスト自体は通る
- **ヘッドレスなし**（`headless: false`）で実ブラウザ録画、タイムアウト 5〜10分
- **14ブロック中7種をカバー** — Timeline 詳細・高度な FX が不足

## テストギャップ分析（重要度順）

| 優先度 | 未テスト項目 | 理由 |
|--------|------------|------|
| **高** | `generate-clip.ts` の正常系（MP4 出力・解像度・Duration） | 動画生成のコア機能。壊れると全パイプラインが止まる |
| **高** | Videos API の正常系（生成→ステータス遷移→一覧取得） | 認証チェックのみで、実際の動画生成フローが未検証 |
| **中** | `youtube-upload.mjs` のメタデータ・エラーハンドリング | API クォータ切れ・通信エラー時の挙動が未検証 |
| **中** | `obs-stream.mjs` の接続エラー・切断復帰 | 録画テスト内では try-catch で握りつぶしている |
| **低** | 大規模シナリオ（100+ブロック）の録画パフォーマンス | 現状のプロジェクト規模では問題になりにくい |

## 教訓

- 録画テストは「デモ動画作成ツール」として機能しており、品質保証テストとは役割が異なる。混同しないことが重要
- Videos API は認証テストのみで正常系が空白。API の中核ロジック（ステータス遷移、ファイル生成）のテスト追加が最優先
- YouTube 投稿はモックテストで十分カバー可能（実際の API 呼び出しは不要）

---

> この文書は Claude Opus 4.6 が生成しました。内容の正確性は人間のレビューを経て確認してください。
>
> Claude Opus 4.6 — Anthropic
