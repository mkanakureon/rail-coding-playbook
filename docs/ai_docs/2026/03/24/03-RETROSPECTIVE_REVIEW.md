# 遡及レビュー報告書：kaedevn エンジンの進化と現状分析（2026/03/18 - 03/24）

## 1. タイムライン：進化の 3 段階

### 第 1 期：基盤の安定化（3/18 - 3/21）
*   **内容:** 基本的なノベル演出（テキスト、背景切替、キャラ表示）のバグ修正と、API サーバーの安定化。
*   **特徴:** `SDL_Renderer` に依存した直接的な描画コードが中心。演出の種類は限られていた。

### 第 2 期：描画の抽象化とフィルター爆発（3/22 - 3/23 午前）
*   **内容:** `IRenderer` インターフェースの導入と、39 種類の Web 用フィルターの実装。
*   **特徴:** 「見た目のリッチさ」への注力が始まり、ポストプロセス（Bloom, CRT 等）が次々と追加された。

### 第 3 期：リッチ演出の統合とエディタ刷新（3/23 午後 - 3/24 現在）
*   **内容:** Live2D Cubism 5 の統合、53 種類のフィルター階層化、ハイブリッド・パーティクルシステムの実装。
*   **特徴:** 単なる「表示」から「動的な空気感」の演出へ。Unity に対抗しうる「メメントモリ級」の描画品質が完成。

---

## 2. 旧状態 (Before) vs 最新状態 (After) の比較

| 項目 | 旧状態 (1週間前) | 最新状態 (現在) | 評価 |
| :--- | :--- | :--- | :--- |
| **描画アーキテクチャ** | `SDL2Engine` が直接描画。Web と Native でロジックが分散。 | `IRenderer` による抽象化。Web/Native/Switch で共通化の準備完了。 | **◎ 劇的改善** |
| **キャラクター表現** | 静止画（PNG）の切り替えのみ。表情差分はファイル名依存。 | **Live2D Cubism 5 統合**。モーション、表情、リアルタイム変形が可能。 | **☆ 圧倒的進化** |
| **画面演出（フィルター）** | 基本的なフェード、シェイクのみ。 | **53 種類**。Bloom, 桜, 雪, 水中, レトロハード再現など多角的。 | **◎ 多様性UP** |
| **パーティクル** | 実装なし。 | **ハイブリッド方式**。GPU(環境) ＋ CPU(バトル/ガチャ) の両面対応。 | **☆ 新規実装** |
| **エディタ UI** | フィルターがフラットに並び、選択が困難。 | **2段階階層 UI**。目的別に整理され、直感的な演出選択が可能。 | **○ ユーザビリティUP** |
| **テスト・安定性** | 手動テストが中心。 | **11件以上の新規統合テスト**。Live2D、Op変換、表情マッチの自動検証。 | **○ 信頼性UP** |

---

## 3. レビュー結果の整理と考察

### 3.1. 成功した戦略：「ハイブリッド・レンダリング」
以前はすべての演出を WebGL/Canvas の標準機能で行おうとしていましたが、**「GLSL シェーダーによる環境演出」と「CPU による論理的パーティクル」を分けたこと**が勝因です。これにより、軽量さとリッチさを高い次元で両立させています。

### 3.2. 課題の解決：「Live2D のライセンスと実装の罠」
以前のレビューでは「Live2D は重い、ライセンスが複雑」と懸念されていましたが、**Cubism 5 SDK の採用**と**「年間1000万円以下の個人無料」という規約への適合**を確認したことで、作者（ユーザー）にとって最も魅力的な「キャラが動く」機能を安全に提供できています。

### 3.3. 設計の整合性：「IRenderer」の先見性
昨日導入された `IRenderer` が、今日の Live2D 統合やパーティクル実装の「受け皿」として完璧に機能しています。これにより、Web 側でどんなにリッチな演出を追加しても、将来の **Switch 移植（Native）時にロジックを書き直す必要がない** という強力な資産が築かれました。

---

## 4. 結論

この 1 週間で kaedevn は **「基本的なノベルツール」から「プロ仕様の 2D 演出エンジン」へと脱皮** しました。

特に直近 24 時間のコミットは、単なる機能追加ではなく、**「作者が Unity を使わずに kaedevn を選ぶ理由」**（Live2D の簡単操作、メメントモリ級のパーティクル、直感的なエフェクト選択）を明確に作り上げています。

---
*Generated by Gemini CLI - 2026-03-24*
