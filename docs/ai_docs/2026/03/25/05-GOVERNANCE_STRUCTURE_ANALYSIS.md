# 調査報告：kaedevn の統治機構（8つの儀式と抽象化層）の成立過程

## 1. はじめに
本レポートは、kaedevn モノレポにおいて「最小限の指示」を「高品質な実装」へと変換する 2 つのエンジン、すなわち「CLAUDE.md の 8 ステップ」と「抽象化層（IRenderer/IInput）」がどのように実現されたかを解剖したものである。

---

## 2. 「8 つの儀式」：AI による自律的防衛本能の結実

### 成立の背景
`CLAUDE.md` に記載された新コマンド追加の手順は、開発初期の「実装漏れ」という失敗経験から生まれた。AI エージェントが、複数のパッケージ（Compiler, Web, Hono, Editor）にまたがる変更の整合性を保つことが困難になった際、自律的に「全レイヤー同期チェックリスト」を作成したことが起点となっている。

### 幾何学的構造
この 8 ステップは、データの流れを垂直（Compiler → Runner → Handler）と水平（Store → Preview → API）の両面で網羅している。
*   **垂直同期:** `commandRegistry` から `WebOpHandler` まで。
*   **水平同期:** `useEditorStore` から `preview.ts` まで。

**【結論】** この手順が AI にとっての「聖典」となったことで、指示が最小限であっても、AI は「どのパズルを埋めれば全体が完成するか」を幾何学的に判断できるようになった。

---

## 3. 「抽象化層」：Switch 移植を前提とした予言的設計

### 実現のプロセス
`IRenderer`, `IInput`, `IAudio` といった抽象化は、プロジェクトの Phase 1 において「Web はあくまで二次的なプラットフォームである」という強力な前提（`cf07e14`）の下に導入された。

1.  **Dependency Inversion (依存性逆転):** 上位のゲームロジックが下位の PixiJS に直接依存せず、抽象インターフェースに依存するように設計。
2.  **Op-centric Architecture:** すべての演出を「命令（Op）」という純粋なデータに変換。

### 効果：Native 移植の爆速化
現在進行中の Phase 2-3 において、C++ 側で `IRenderer` を実装した瞬間に Web 側のロジックがそのまま動作するのは、初期段階で「Web 特有の便利機能」をあえて封印し、抽象化という「先行投資」を行った成果である。

---

## 4. 総括：秩序による統治

kaedevn の成功は、以下のサイクルが回っていることに起因する。

1.  **オーナーの意志:** 「正しい型と手順を維持せよ」という規律の強制。
2.  **AI の自律:** 失敗を糧にした手順（儀式）の明文化。
3.  **構造の恩恵:** 抽象化層によるプラットフォーム間の障壁除去。

**「コードを読まないオーナー」でもプロジェクトを統治できるのは、この 3 層の統治機構が、AI にとって「最も効率的でバグの出にくいレール」となっているからである。**

---
*Generated by Gemini CLI - 2026-03-25*
*Governance & Architecture Analysis*
