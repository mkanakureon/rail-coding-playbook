# 生成パイプライン最適化・実装計画書 (2026-03-12)

本ドキュメントは、AI生成パイプラインから出力される `editor-json` 等の成果物を、ブロックエディタへよりシームレスかつ高品質にインポートさせるための、生成AI側の対策計画である。

## 1. 責務の定義
- **Gemini CLI (当エージェント)**: 生成ロジックの改善、プロンプトへの制約注入、一貫性のある JSON 成果物（Stage 0-4）の出力までを担当。
- **Claude Code (外部ツール担当)**: インポートスクリプトの修正、エディタ表示の確認、Playwright テストの拡張・実行を担当。

## 2. 実装対策案（生成AI側）

### 2-1. キャラクターIDの揺れ防止 (Stage 3 & 4)
- **現状**: `garud / gard` のような綴りのブレが発生している。
- **対策**: `assist-cli.ts` の執筆プロンプトに、設定済みのスラグリストを「使用可能 ID 一覧」として動的に挿入する。
- **成果物**: `LineSchema` に合致し、かつ既存キャラと完全一致した `characterId` を持つ JSON。

### 2-2. 背景 ID のクリーンアップ (Stage 4)
- **現状**: `$bg:` プレフィックスが残存し、エディタ側での置換が必要。
- **対策**: Stage 4 の JSON 変換時に正規表現を用いてプレフィックスを除去し、`manifest.json` と直接紐付く ID に統一する。
- **成果物**: エディタがそのまま画像パスを解決できる `bg` ブロックデータ。

### 2-3. 未定義キャラクターの自動プロビジョニング
- **現状**: `characters.json` にメインキャラ以外のデータが不足している。
- **対策**: ストーリーに登場した全サブキャラを Stage 4 で抽出し、公式アセット（老人、悪役、村人等）から属性の近い画像を自動割り当てして `characters.json` を更新出力する。
- **成果物**: 登場人物全員に適切な画像が設定された `characters.json`。

## 3. アセット・マッピング戦略（案B準備）
- **調査**: `GET /api/official-assets` の結果に基づき、物語上の抽象描写を具体的アセット ID へ変換する「セマンティック・マッピング・テーブル」を生成AI内部に定義する。
- **役割**: AI が「燃える村」と出力したら、自動的に `bg_village_fire`（またはそれに近いもの）を割り当てる。

## 4. Playwright E2E テスト拡張案（Claude Code への提示）
- **サムネイル網羅性テスト**: エディタの全ページを巡回し、背景・キャラ画像が「未選択」状態でないことを検証する。
- **プレビュー・整合性テスト**: API プレビューを開き、セリフ送りに伴って背景・キャラが JSON の定義通りに切り替わるかを、DOM の属性変化から検証する。

---
*Generated by Gemini CLI - 次のステップ: 本計画書に基づく assist-cli.ts のプロンプト調整（指示待ち）*
