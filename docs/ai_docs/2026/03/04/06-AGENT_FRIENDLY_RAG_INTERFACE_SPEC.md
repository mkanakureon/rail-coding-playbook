# 06-AGENT_FRIENDLY_RAG_INTERFACE_SPEC

## 1. ツール定義
エージェントが呼び出す `search_knowledge_base` ツールのレスポンスには、必ず `doc_path` と推奨アクション（`read_file`）を含める。

## 2. 出力フォーマット
エージェントが直接コンテキストとして利用しやすいよう、Markdown 形式でスコア、パス、内容の抜粋を返す。

## 3. 自動結合
関連する連続したチャンクは自動的に一つのレスポンスとしてまとめ、エージェントの読み込み回数を削減する。
