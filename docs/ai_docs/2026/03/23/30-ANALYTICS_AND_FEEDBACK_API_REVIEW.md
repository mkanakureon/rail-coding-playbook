# Code Review: fd6dc19 & 4caddf8 (2026-03-23)

## Commit Info
- Hash: fd6dc19a916af545039376fd8b42fbede7e67266
- Subject: feat: 読了アナリティクス API + フレッシュ・ピックアップ（sort=fresh）
- Hash: 4caddf8e66eaf51ba9f727002b9bf836daf7b393
- Subject: feat: 読者フィードバック（シーン・リアクション + 読了チップ）API 実装

## Summary
作者の継続モチベーションを最大化するための「定量データ（離脱分析）」と「定性フィードバック（リアクション）」の API 基盤実装。

## Findings (fd6dc19: アナリティクス)
1. **戦略的データ構造**: ページインデックスごとの閲覧数と滞在時間を `BigInt` で保持。離脱ファネル（ボトルネック）の特定に特化した、無駄のない設計。
2. **新着ブーストの実装**: `sort=fresh` による「プレイ数 50 未満」の優先表示ロジックは、初期作者の「ゼロ再生」という最大の挫折要因をシステムで解決する優れた一手。

## Findings (4caddf8: フィードバック)
1. **「管理コストゼロ」の哲学**: テキストコメントをあえて排除し、絵文字リアクションと定型タグ（チップ）に絞った設計。モデレーション負荷を回避しつつ、ポジティブなフィードバックのみを作者に届ける「安全な場所」を構築。
2. **効率的集計**: Prisma の `groupBy` を活用したページ別・種別別の集計ロジックにより、フロントエンドでの高度な可視化を容易にしている。

## Recommendations
- **ユニーク性の担保**: 正確な離脱率を出すため、IPハッシュや Cookie を用いたセッションベースのユニークカウント（重複排除）の導入を検討すべき。
- **プライバシー配慮**: `sessionId` を平文で保持している点について、プライバシーポリシーへの記載、またはハッシュ化による匿名化を推奨。
- **将来のスケーリング**: ページ遷移ごとの `upsert` は、MAU 1万超えの際のリスクになる。ロードマップ Phase 2 で予定している Redis へのバッファリング移行を常に視野に入れておくべき。
