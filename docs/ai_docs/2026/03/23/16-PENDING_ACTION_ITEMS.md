# TODO: 今後の対応が必要な改善項目一覧 (2026-03-23 時点)

このドキュメントは、過去2週間のコードレビューおよび最新リポジトリとの照合により抽出された、今後の実装・改善が必要なタスクの一覧です。

## 1. 最優先事項 (Priority: High)

### 1-1. スケーラビリティ & パフォーマンス (Phase B/C)
- [ ] **動画生成のジョブキュー化**: `apps/hono/src/routes/videos.ts` での `exec` 直接呼び出しを、Redis + BullMQ 等を用いた非同期ジョブキュー方式へ移行する（サーバー負荷集中時のパンク防止）。
- [ ] **認証の「二段構え」設計**: JWT claims による高速化は実現済みだが、決済や作品公開等のクリティカルな操作時のみ DB を強制参照し、アカウント停止等の状態をリアルタイムに反映させる。
- [ ] **カーソルベース・ページネーション**: 作品一覧取得において、`skip` 方式から `cursor` 方式へ移行し、データ増大時のクエリ速度低下を防止する。
- [ ] **テレメトリ設定の共通化**: `apps/hono` に散在する OpenTelemetry の設定ロジックを `packages/telemetry`（新規）に抽出し、モノレポ全体で再利用可能にする。

### 1-2. モバイル・UI 対応
- [ ] **セーフエリア (Safe Area) 対応**: `PlayLayout` 型および `applyPlayLayout` ロジックに、iPhone 等のノッチを考慮した `safeAreaInset` の概念を導入する。
- [ ] **イベント駆動型キャプチャ**: タイトル画像生成時の待機時間を `setTimeout` 固定値（現在6秒）から、エンジン（web）側からの「描画完了メッセージ」を待機する方式に変更し、低スペック端末での失敗を防止する。

## 2. 機能拡張 & 品質向上 (Priority: Medium)

### 2-1. エディタ UI/UX
- [ ] **フィルターの重複・制約バリデーション**: `filter_mix` において「天候系フィルターは1つまで」といった設計上の制約をエディタ側で警告表示する仕組みの実装。
- [ ] **型安全性の強化**: `resolveAssetUrl` 等、引数の数や型が曖昧な関数のシグネチャを明確にし、ESLint の `no-explicit-any` 等を厳格化する。
- [ ] **キャラクター型の変換ロジック共通化**: `Character` と `ChClassCharacter` の相互変換ロジックが散在しているため、共通のユーティリティ関数に集約する。

### 2-2. 拡散性 & マーケティング
- [ ] **動的 OGP 画像の高度化**: 作品サムネイルに作品タイトルを自動合成した OGP 画像を動的に生成する（Vercel OG 等の検討）。
- [ ] **シーンへのディープリンク**: シェア用 URL にページ番号やシーン ID を含め、リンクを開いた瞬間にそのシーンから開始できる仕組みの強化。

### 2-3. 自動化 & 運用
- [ ] **アセット最適化パイプライン**: AI 生成アセットが増大しているため、アップロード時や生成時に WebP への自動変換およびリサイズを行う非同期処理の導入。
- [ ] **モニタリングダッシュボード**: OTel で収集したメトリクスを可視化する Grafana/Jaeger の正式なセットアップと、DB 接続プール詰まり等のアラート設定。

## 3. ドキュメント & テスト (Priority: Low)

- [ ] **アーキテクチャ図の更新**: `filter_mix` や新設された `packages/core` 内の共通型定義を反映した最新の設計図への更新。
- [ ] **テスト自動化の拡大**: 「要統合テスト」に分類されている動画生成やポップアップ操作の自動化率向上。
- [ ] **フィルターサンプル集**: 各画面フィルターの視覚的な Before/After サンプルを開発者向けドキュメントに拡充。
