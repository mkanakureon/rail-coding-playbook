# アセット最適化：WebP 自動変換の現状と検証

**作成日**: 2026-02-26
**対象**: `apps/hono` の実装整合性とネイティブ対応
**核心**: 既に実装済みの最適化機能の確認

## 1. 現状の実装確認結果 (Status Quo)
`apps/hono/src/routes/assets.ts` のソースコード精査により、以下の機能が既に稼働していることが確認されました。

- **自動 WebP 変換**: アップロード時に `optimizeImage` ユーティリティが呼び出され、サイズが縮小される場合は WebP 形式で保存されます。
- **インテリジェント・リサイズ**: 高解像度アセット（2048px超）は、エンジンのパフォーマンス維持のため自動的にリサイズされます。
- **コンテンツ・ハッシュ**: ファイル名が内容のハッシュ値に基づいているため、同一内容の重複保存が防がれています。

## 2. ネイティブエンジン側への影響
`SDL_image` は WebP を標準サポートしていますが、以下の点を確認する必要があります。

- **`IMG_Init`**: `main.cpp` で WebP サポートが明示的に初期化されているか。
- **テストアセット**: `packages/native-engine/assets/` 内に WebP 画像を配置し、正常に描画されるか。

## 3. 次の検証 Step
1.  **ネイティブテスト**: `main.cpp` で `IMG_Init(IMG_INIT_PNG | IMG_INIT_WEBP)` を追加し、動作確認。
2.  **同期スクリプトの調整**: `sync-native.sh` で拡張子（.webp）を正しくハンドリングできているか。

---
*Verified by Gemini CLI Full-stack Engineer.*
