---
generated_by: Gemini CLI
date: 2026-04-01
type: report
project: kaedevn-monorepo
---

# 欠落テスト分析報告書 (エディタ・スマホ・動画編)

## 1. ノベルゲームエディタ
エディタ機能は「複雑な状態遷移」と「スクリプト互換性」が生命線だが、自動テストが極めて薄い。

- **状態管理 (`useEditorStore`)**:
    - [ ] ブロックのネスト移動（If文の中にセリフを入れる等）の整合性テスト。
    - [ ] 大規模プロジェクト（1,000ブロック以上）読み込み時のパフォーマンスとメモリリークテスト。
- **スクリプト変換**:
    - [ ] `ksConverter.ts`: KAG/KS スクリプトからブロックUIへの変換の完全性テスト。
- **UI/UX**:
    - [ ] `CommandPalette`: キーボードショートカット (`Cmd+K`) による操作の自動化テスト。

## 2. スマホ・モバイル UI
`useMediaQuery` による分岐が多数確認されたが、スマホ実機を想定した検証が不足している。

- **タッチジェスチャー**:
    - [ ] `useLongPress.ts`: 長押しによるメニュー表示の単体テスト。
    - [ ] ドラッグ＆ドロップ: タッチ操作時のブロック並び替えテスト（Playwright mobile mode）。
- **レスポンシブ・レイアウト**:
    - [ ] **Safe Area インセット**: `env(safe-area-inset-*)` が反映され、FAB（浮遊ボタン）がシステムバーと重ならないかの検証。
    - [ ] **Mobile Bottom Sheet**: モバイル版 `PageListModal` 等が正しいアニメーションで表示されるかの検証。

## 3. 動画作成 & YouTube 自動投稿
「動画出力（録画）」と「外部投稿」は、ヘッドレスブラウザの動作安定性と API 認証がボトルネックとなっている。

- **動画レンダリング (scripts/stream/record-demo.ts)**:
    - [ ] **Duration 制御**: 指定された秒数で録画が確実に終了し、ファイルが壊れていないかの検証。
    - [ ] **解像度整合性**: エディタで指定した解像度（1280x720 等）が録画ファイルに正しく反映されているかの検証。
    - [ ] **無音録画の防止**: 音声アセットが読み込まれなかった場合に、録画を中断するか警告を出すロジックのテスト。
- **YouTube 投稿 (scripts/stream/youtube-upload.mjs)**:
    - [ ] **API クォータ・リミット**: 投稿失敗時（クォータ切れ、通信エラー）の再試行（Retry）ロジックの検証。
    - [ ] **メタデータ整合性**: タイトル、説明文、タグが API リクエストに正しくマッピングされているかのモックテスト。
- **エディタ連携**:
    - [ ] エディタから Hono API を経由して録画ジョブ（Queue）が正しく発行されるかの統合テスト。

## 4. 提言
特に **「スマホ環境でのタッチ操作」** と **「エディタのブロックネスト操作」** は、日産1万行のペースで機能追加を続けると、デグレが発生した際のダメージが極めて大きい。
早急に Playwright のモバイルエミュレーションを使用した E2E テストの拡充を推奨する。
---
