---
generated_by: Gemini CLI
date: 2026-04-01
type: report
project: kaedevn-monorepo
---

# 欠落テスト分析報告書

ソースコードの規模（約1,000ファイル）に対し、テストファイルが148件（約15%）に留まっており、特にフロントエンド・UI層のテストが著しく不足している。

## 1. 優先度：極大（即時追加が必要）

### apps/next (Next.js Portal)
- **現状**: 102ファイル中、テストはわずか3件。`app/` ディレクトリ（App Router）のテストが 0 件。
- **欠落内容**: 
    - ユーザー認証フロー（Login/Register/Session）の結合テスト。
    - プロジェクト一覧・詳細ページのレンダリングテスト。
    - API Routes / Server Actions の単体テスト。

### apps/editor (Visual Editor)
- **現状**: 146ファイル中、テストは13件。
- **欠落内容**:
    - `useEditorStore` の複雑な状態遷移（ブロックの追加・削除・ドラッグ）の単体テスト。
    - コマンドパレットやサイドバーコンポーネントの表示・動作テスト。
    - 保存・読込（persistence）ロジックの異常系テスト。

## 2. 優先度：大（リリース前に強化が必要）

### packages/web (Engine Core)
- **現状**: 177ファイル中、テストは10件。
- **欠落内容**:
    - PixiJS 描画パイプラインのモックテスト。
    - 解像度変換、レイアウト計算ロジックの境界値テスト。
    - アニメーション、トランジションの完了フラグの信頼性テスト。

### packages/core (Shared Types & Logic)
- **現状**: 63ファイル中、テストは10件。
- **欠落内容**:
    - セーブデータスキーマのマイグレーション関数のテスト（互換性担保）。
    - 共通ユーティリティ（IInput, IStorage 抽象の共通実装）のテスト。

## 3. テスト品質の問題（01-CODE_AUDIT_REPORT 参照）
- **flaky tests**: `waitForTimeout` が 82 ファイルに散在しており、CI の成功率を下げている。
- **skipped tests**: 57 件のテストがスキップされたまま放置されており、回帰バグの温床となっている。

## 4. 総合テスト計画（プロトタイプ）

1. **Phase 1: インフラ安定化**
    - `waitForTimeout` を `waitForSelector` 等に置換。
    - スキップされたテストの全復旧。
2. **Phase 2: 重点カバレッジ強化**
    - `apps/editor/src/store` のカバレッジを 80% まで引き上げる。
    - `apps/next` のクリティカルパス（Auth/Project CRUD）の E2E を追加。
3. **Phase 3: 結合・性能テスト**
    - プロジェクト保存→読み込み→プレビューの一貫したフローを自動化。
    - 大規模スクリプト（1万行超）読み込み時のメモリ・CPU負荷計測。

---
