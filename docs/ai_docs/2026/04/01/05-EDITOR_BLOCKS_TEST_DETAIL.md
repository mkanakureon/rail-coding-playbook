---
generated_by: Gemini CLI
date: 2026-04-01
type: test-detail-spec
project: kaedevn-monorepo
---

# エディタシステム & 個別ブロック詳細テスト仕様書

## 1. エディタ・システム機能の検証

### 1.1 画面（タブ）遷移と統合
- **[Flow] `SceneFlowGraph`**: 
    - テストケース: `jump` ブロックが複数あるプロジェクトを読み込み、グラフのノードとエッジが正しく接続されていることを検証。
- **[Layout] `LayoutEditor`**:
    - テストケース: UI要素をドラッグして座標を `x: 100` に変更した際、`useEditorStore` の `layout` 状態が即座に更新されることを検証。
- **[CharGen] `CharacterGenerator`**:
    - テストケース: 髪型・目・服のパーツを選択し、合成された Base64/Blob データが期待通り生成されるかのロジックテスト。

### 1.2 高度なUI操作
- **[ContextMenu] コンテキストメニュー**:
    - テストケース: ブロックを右クリック -> 「コピー」 -> 別の場所で「貼り付け」を行った際、ID が重複せずに（新規IDで）複製されることを検証。
- **[CommandPalette] コマンドパレット**:
    - テストケース: `Cmd+K` 入力 -> 曖昧検索（例: "bg" で "Background" がヒット） -> `Enter` でカレント位置に挿入されることを検証。

---

## 2. 個別ブロック（コマンド）の深掘り検証

### 2.1 演出・ビジュアル系
- **`ScreenFilter` / `FilterMix`**:
    - [ ] プリセット（セピア、モノクロ、ピクセル化）の選択が `Op` に正しく反映されるか。
    - [ ] 2つのフィルタの混合率（`ratio`）の境界値テスト（0.0 ~ 1.0）。
- **`ColorAdjust`**:
    - [ ] スライダー操作による明度・コントラストの変更がリアルタイムにプレビューに反映されるか。

### 2.2 アニメーション・制御系
- **`Camera`**:
    - [ ] イージング（Linear, EaseIn, EaseOut）の選択肢が正しく保存されるか。
- **`Timeline`**:
    - [ ] トラックの追加・削除と、キーフレームのドラッグによる時間移動の整合性テスト。
- **`Live2DCh`**:
    - [ ] 表情（Expression）とモーション（Motion）のコンボボックスが、選択されたモデル ID に基づいて正しくフィルタリングされるか。

### 2.3 高度なロジック
- **`Call` / `Jump`**:
    - [ ] 存在しないラベルを指定した場合のバリデーションエラー表示。
    - [ ] シーンを跨ぐジャンプの設定整合性。

---

## 3. 実装上の注意（CAO 監査メモ）
- **状態の肥大化**: `useEditorStore` が 1,000 行を超えているため、各アクションのテストは `Vitest` で「純粋な状態遷移」として分離してテストすることを推奨。
- **スマホ解像度での UI 消失**: `MOBILE_HIDDEN_BLOCK_TYPES` に含まれるブロック（`timeline`, `ksc` 等）が、スマホ表示時にパレットから確実に消えていることを Playwright で検証すること。

---
*設計責任者: Gemini CLI (CAO)*
