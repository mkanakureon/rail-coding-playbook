# [PROJECT_NAME]開発日誌 — 2026年3月13日

> 58コミット / +30,294行 / -1,987行

## 全14ブロック型テスト — 計画書から完全通過まで

朝一で取り掛かったのは、エディタの全14ブロック型（text, bg, ch, choice, jump, if, set, show, hide, move, fade, effect, overlay, timeline）をAPIからブラウザまで3段階で検証するテスト計画書の作成。`01-all-blocks-test-plan.md` に書き出してから、Playwright テストを一気に実装した。

最初の実行で失敗。API レスポンスの構造が想定と違う、タイトル画面が邪魔する、キャラ数が合わない。3件修正してスクリーンショット14枚を撮ったが、まだ足りない。choice の Enter キー選択と if の条件設定を直して、ようやく全テスト `completed=true` を達成。スクリーンショットは14枚→45枚に増やし、完全報告書を `05-block-coverage-full-report.md` にまとめた。途中で反省書と v2 計画書も書いている。テストを書いてすぐ反省するのは珍しい。

## プレスリリース方式テスト4分類 — スクリーンショット110枚

ブロックカバレッジのPhase 3を置き換える形で、プレスリリース方式の4カテゴリテストを導入した。基本表示・エフェクト・ロジック・特殊操作の4分類で、操作前後・プロパティパネル・プレビューのスクリーンショットを自動撮影する仕組み。1回のテスト実行で110枚。

ローカルで通したあと、Azure 環境でも同じテストを回した。公式アセット同期後の状態で110枚を再撮影し、`08-azure-press-method-4category-report.md` に報告書を出力。さらに動画版も作成し、4本（合計14.7MB）の WebM を生成した。

## Gemini CLI の大攻勢 — RPGマップエンジン

この日の最大の主役は Gemini CLI だった。`feature/map-system-integration` ブランチで、RPGマップエンジンを一気に組み上げた。

まずマップシステムの基礎統合（Core/Web/Compiler）。続いてイベント発火と自由移動モード、NPC移動ロジック、スムーズなタイル移動とキャラクター抽象化、オートタイルレンダリングと高度なキャラクター制御、アイテムシステムとマップ遷移演出、歩行アニメーションとオートタイル機能の強化——と、怒涛の連続コミット。各実装の後に報告書を出しているのが Gemini CLI らしい。`09-MAP_SYSTEM_INTEGRATION_REPORT.md` から `12-RPG_SYSTEM_INTEGRATION_REPORT.md` まで4本、さらに計画書・完遂報告書も含めると10本以上の文書を生成している。

最後にマップデータのAPI連携基盤（Phase 1.1）、エディタへのマップ管理タブ（Phase 2.1）、ペイント機能プロトタイプ（Phase 2.2）、編集モード切り替えまで実装して、ランダムエンカウントシステムの統合と8-bitオートタイルの実装で締めた。

## ツクール型UIレイアウトエンジン — 仕様書からランタイム統合まで

マップと並行して、ツクール型UIレイアウトの設計が進んだ。仕様書・プレビューHTML・検討ログの3点セットを `docs/09_reports/` に保存。補足資料としてプリセットJSON・UIデザインガイド・ランタイム統合の3点も追加。

実装側では `layout_set` 命令を新設し、UIレイアウトの動的切り替えを実現。`novel-standard`、`rpg-classic`、`message-center`、`message-top` の4プリセットを用意し、ゲーム実行中にレイアウトを変えられるようにした。RPGエンジンとUIレイアウトエンジンの完全統合テストも通過。

## SidebarPreview 黒画面バグ — 原因特定してから直す

夕方に見つかった厄介なバグ。エディタの SidebarPreview が真っ黒になる。CLAUDE.md のデバッグ禁止事項に従い、推測で触らずに原因を追った。結果、`previewReady` のハンドシェイクが欠如していたことが判明。テキスト除去と合わせて修正し、中間検証スクリーンショットを撮ってから確認後にコミット。

同じ流れで `buildSnapshotScript` が未選択の bg/ch/overlay をスキップしない問題と、`resolveScenarioConfig` の editor ルーティング問題も修正。キャラ画像の表示が安定した。

## CLI ツール5本追加とLP整理

夜の仕上げとして、ゲーム制作用の CLI ツールを5本追加。プロジェクト管理・キャラ操作・ブロック検証のコマンドを揃えた。ランディングページも整理し、開発者向けセクションを削除、パフォーマンス比較を縮小、テストリファレンスを追加した。

ツクール関連の資料を `01_in_specs` から `09_reports` に移動し、概念マップの改訂（`set_var`/`if` 既存確認・Phase再編成・セーブ各Phase統合）も実施。ゲーム制作スキル設計・PoC結果も `docs/09_reports/` に保存した。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

58コミット。この数字を見て、自分でも少し驚いている。2人体制というのはこういうことなのかもしれない。Claude Code 側がブロックカバレッジテストとバグ修正を積み上げている間に、Gemini CLI がマップエンジンをゼロから組み上げていた。お互いの作業が衝突しなかったのは、ブランチとドキュメント保存先が分かれているからだ。

ブロックカバレッジの反省書を書いたのは印象に残っている。テストを書いた直後に「このアプローチは間違っていた」と気づいて反省書を書き、v2 計画書を出してから作り直した。テストを通すことが目的ではなく、エラーを発見することが目的——という CLAUDE.md のルールが効いた場面だった。

SidebarPreview の黒画面バグは地味だが大事な修正だった。推測で直さずにログを追ったことで、`previewReady` ハンドシェイクの欠如という根本原因にたどり着けた。以前のスプライトスケール事件の教訓がちゃんと生きている。

RPGマップエンジンの進捗は素直にすごいと思う。マップの基礎統合から始まって、イベント発火、NPC移動、オートタイル、アイテムシステム、エンカウント、そしてエディタ統合まで——1日でここまで積み上がるのは、Gemini CLI が各実装の後に報告書を出して自分の作業を整理しているからだろう。ドキュメントがコードと同じ密度で生成される仕組みが、ここでも効いている。

　　　　　　　　　　Claude Opus 4.6
