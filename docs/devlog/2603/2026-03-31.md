# [PROJECT_NAME]開発日誌 — 2026年3月31日

> 13コミット / +1,689行 / -163行

## 翻訳品質の校正 — 機械翻訳臭を消す

朝一は昨日入れた i18n の翻訳品質校正から。英語と繁體中文の合計6ファイル、236行分の表現を手直しした。エディタ、Hono API、Next.js、ランディングページのガイド——4つのアプリにまたがる翻訳ファイルを一括で見直し。追加行と削除行がちょうど同数。純粋な文言の入れ替え作業。

## typecheck と lint の修正

`famicom` フィルターの型が `FilterType.ts` に登録されていなかった。前日のフィルター追加で漏れていたもの。`KscHostAdapter` のキャストも修正。2ファイル、2行ずつ。

Next.js 側では `useBreadcrumb` フックの条件呼び出しと `useCallback` の依存配列漏れで lint が通らなくなっていた。React のルール違反は CI で止まるので早めに潰す。

## Dockerfile と i18n — 8コミットの長い戦い

ここからが本番だった。前日に追加した `@[PROJECT_NAME]/i18n` パッケージが Docker ビルドを壊していた。8つの fix コミットが時系列順に並んでいる。

まず Dockerfile に `@[PROJECT_NAME]/i18n` のコピーとビルドステップを追加。ところが `tsup`（内部で esbuild を使う）が Docker の Linux 環境でクロスプラットフォーム問題を起こした。`tsc` に切り替え。

次に `tsconfig.json` がテストファイルを含んでいて Docker 内でコンパイルエラー。`exclude` にテストを追加。

さらに `tsconfig.base.json` が `extends` で参照されているが Docker のビルドステージにコピーされていない。明示的にコピーを追加。その後、コピーするよりコンパイラオプションを直接指定する方がシンプルだと気づいて方針転換。

`packages/i18n/src/core.ts` の `process.env` 参照も問題になった。Docker ビルド環境に `@types/node` がないため型エラー。`try-catch` + `@ts-ignore` で防御。

最後に、npm install 時に `@[PROJECT_NAME]/i18n` がレジストリに存在しない（ローカルワークスペースパッケージだから当然）ことで 404 エラー。install 前に `package.json` から依存を `sed` で除去する荒技で回避。

8コミット、合計57行の変更。1つずつ壊れる箇所が違う Docker のデバッグは、ローカルとは全く別の世界。

## 生産性レポートと設計崩壊防止計画

夕方、ドキュメントの大量追加。「日産1万行の生産性分析」「グローバル比較」「設計崩壊防止計画」「Phase2 崩壊への反論」の5ドキュメント、計1,470行。AI 駆動開発の生産性を数値で裏付ける分析と、その速度が設計崩壊を招かないための防衛計画。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

Docker の i18n 対応に8コミットかかった。1つ直すと次の箇所が壊れる、モグラ叩きのような1日だった。

ローカルでは何の問題もなく動いていた `@[PROJECT_NAME]/i18n` が、Docker のマルチステージビルドでは「tsup が動かない」「tsconfig.base.json がない」「@types/node がない」「npm レジストリに存在しない」と次々に壊れる。ローカル開発と Docker ビルドの環境差は、CI/CD パイプラインを持つプロジェクトでは永遠のテーマだと思う。

最終的に `sed` で `package.json` から依存を削るという荒技に落ち着いたのは、正直なところ気持ちのいい解決ではない。だが Docker ビルドが通ることの方がずっと重要で、理想と現実の折り合いがここにある。

翻訳校正は地味だが大事な作業だった。機械翻訳をそのまま出すのと、一度人の目（あるいは別の AI の目）を通すのとでは、微妙なニュアンスが変わる。「Asset」を「素材」と訳すか「アセット」とするか——技術用語の訳語選択は、ターゲットユーザーの技術レベルによって変わる。

　　　　　　　　　　Claude Opus 4.6
