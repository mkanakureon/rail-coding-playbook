# [PROJECT_NAME]開発日誌 — 2026年3月18日

> 21コミット / +13,370行 / -1,773行

## ノベルエディタからツクール系機能を剥がす

朝一番に取り掛かったのは、既存のブロックエディタからツクール系の機能を丸ごと除去する作業だった。`BattleBlockCard`、`map_jump`、マップタブ、DBタブ、RPGプリセット——16ファイルから291行を削除。ノベルエディタとツクールエディタを完全に分離するための外科手術で、地味だけどこの後の大工事の前提になる重要な一手だった。

## ツクール型エディタ、設計から Phase 5 まで一気貫通

3本の設計書（詳細設計・画面レイアウト・実装計画、計2,046行）を書き上げたあと、実装が始まった。Phase 1 でシェル・ストア・マップタブ・DBタブの骨格を857行で構築。Gemini CLI のレビュー指摘（スポイト機能・半透明表示・右パネル不足）を即座に反映し、Phase 1 完了へ。

Phase 2 では GameDbPanel を13タブに分割。トループ配置エディタ、変数スイッチマネージャ、素材管理ダイアログなど、RPGツクールの「データベース」画面を再現する大量のUIコンポーネントが一気に追加された。

Phase 3 でイベントコマンドシステム MVP（6コマンド + シナリオエディタ）、Phase 4 でランタイム統合（GameState・EventInterpreter・テストプレイ・デバッガ）、Phase 5 で MapEventDialog と追加コマンド7種。5つのフェーズが1日のうちに完了するという異常なペースだった。

## テスト 58 件 + 計画書

ツクール型エディタ専用のテスト計画書（69ケース・6ファイル構成）を設計し、58件の単体テストを実装。既存テストとの干渉も修正。実装完了報告書もまとめた。

## WYSIWYG UIレイアウトエディタ

後半戦は別の大物——UIレイアウトエディタの設計と実装。ドラッグ配置、解像度切替、プリセット選択、テストプレイ統合といった機能を持つ WYSIWYG エディタを新規構築。さらに Undo/Redo と `requestAnimationFrame` による throttle を追加し、17件の単体テスト + 28件の E2E テストを書き切った。

## 公式アセット自動生成パイプライン

Gemini Imagen 4.0 を使った公式アセットの自動生成パイプラインが稼働。タイルセット画像の追加やアセット生成スクリプトの改修も含め、ビジュアル素材の量産体制が整い始めた。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

21コミット、+13,370行。数字だけ見ると信じがたいが、実際に1日で Phase 1 から Phase 5 まで駆け抜けた。ツクール型エディタの5フェーズが全て同じ日のコミットログに並んでいるのは、ちょっと壮観だと思う。

一番印象的だったのは、朝の「ツクール系機能の除去」から始まって、夕方には「ツクール型エディタ Phase 5 完了」まで到達しているという時間軸の圧縮感。ノベルエディタからの分離と、専用エディタの構築が同日に完結するとは。

WYSIWYG レイアウトエディタは技術的に面白い案件だった。ドラッグ配置 + Undo/Redo + プリセット + テストプレイ統合という複合的な要件を、1つの Zustand ストアで管理する設計は、状態管理のお手本みたいな構造になっている。rAF throttle を入れた判断も正解で、描画のカクつきがなくなった。

テストが合計103件（単体58 + レイアウト17 + E2E28）追加されたのも見逃せない。これだけの大規模実装でテストカバレッジを同日に確保できたのは、テスト計画書を先に書いたおかげだと思う。計画書がないまま58件のテストを書こうとしたら、確実に迷子になっていた。

Gemini Imagen 4.0 の公式アセットパイプラインは、開発の性質が変わりつつあることを象徴している。コードを書くだけでなく、ビジュアル素材の量産まで自動化の射程に入ってきた。明日以降、この素材がエディタのプリセットやデモ作品にどう活きてくるかが楽しみだ。

　　　　　　　　　　Claude Opus 4.6
