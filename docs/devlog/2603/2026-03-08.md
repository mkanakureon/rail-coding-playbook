# [PROJECT_NAME]開発日誌 — 2026年3月8日

> 42コミット / +10,328行 / -1,686行

## タイムラインのスケール問題を追い詰める

朝いちばんに取り掛かったのはカメラ操作の E2E テスト。Playwright でマイページからブロック追加、ズーム・パン操作、プレイ確認まで通すテストを 383 行で書いた。続けてタイムラインテストにも着手したが、ここで前日から引きずっていたスケール適用のバグが再燃した。

タイムラインで指定したスケール値がスプライトに上書き適用されていて、ベーススケールが無視される問題。`WebOpHandler.ts` でスケール適用を「絶対値セット」から「ベーススケールとの乗算」に修正した。19 行の変更だが、原因特定に至るまでのデバッグがそれなりに長かった。

この件は振り返りドキュメント（134 行のポストモーテム）にまとめて、`CLAUDE.md` にも「推測でコードを変更しない」というルールを 19 行追加した。バグ修正の手順を明文化したのは、同じ失敗を繰り返さないための保険。

## テストファイルとスクリプトの大整理

午前中から午後にかけて、リポジトリの構造を大きく変えた。

まず **テストファイル**。`tests/` 直下に 50 以上のファイルが平置きされていた状態から、`azure/`、`local/editor/`、`local/recording/`、`shared/auth/` など環境別・機能別のサブディレクトリに再配置。83 ファイルの移動で、各テストファイルには AI ヘッダー（テスト概要・前提条件・サーバー要件）も追加した。Playwright の config ファイルも 9 個あったものを 3 つに統合。

次に **`scripts/` ディレクトリ**。60 以上のスクリプトが直下にあったのを、`ai/`、`build/`、`cli/`、`db/`、`debug/`、`deploy/`、`docs/`、`rag/`、`screenshot/`、`stream/`、`test/` の 14 カテゴリに分類。132 ファイルが移動し、各カテゴリに README とテンプレートファイルを配置した。Skill ファイルやシェルスクリプト内のパス参照も全て更新している。

整理後に検証計画を書いて 6 段階でチェック。ユニットテスト 7 件が失敗したので修正し、175 件パスを確認。ルート直下に散らばっていたスクリーンショット画像は `.gitignore` に追加して掃除した。

## スキルの棚卸し

全 18 スキルに `description` フロントマターを追加した。Claude Code がスキルを検出するとき、ファイルを全部読まなくても概要がわかるようにする狙い。壊れた箇所を自動追跡する `broken-memo` スキルも新設。テストが失敗したときに、何が壊れているかを `/tmp` のメモファイルに書き出す仕組み。

## RAG インデックスの自動更新

`rag-index.ts` に差分更新機能を入れた。`dev-start.sh` に組み込んで、開発サーバー起動時にバックグラウンドでインデックスを更新する。cron で毎時実行する設定も追加。移動先の `scripts/cli/rag/` でインポートパスが壊れていたのを直すのに 1 コミット使った。

## Gemini 生成 KS → ブロックエディタ変換

午後の中盤から、Gemini で生成した `.ks` スクリプトをブロックエディタのプロジェクトに自動変換するパイプラインを組んだ。`ks-to-project.mjs` が 306 行。テキストブロックだけでなく、`@bg` / `@ch` / `@set` / `@choice` を適切なブロック型に変換する。発話者フォーカス演出（会話中のキャラだけ明るく表示）も仕込んだ。

`assist-cli.ts` も大幅に書き直して 937 行に。サブコマンド（`generate` / `resume` / `status`）、途中再開、プログレスバー、エラー時の自動リトライを追加。以前の 111 行から 8 倍に膨らんだが、実用に耐える CLI になった。

## ネイティブエンジンが本格的に動き始めた

夕方から夜にかけて、C++ ネイティブエンジン（SDL2）に集中的に手を入れた。

**iOS**: フルスクリーン対応と動的論理解像度。`ios_screen.m` で Safe Area を取得して、SDL2 の描画領域に反映する。`Info.plist` と `LaunchScreen.storyboard` も追加。プロジェクトディレクトリモード（`--project-dir` で assets/ を指定）も実装した。

**Android**: 横画面（landscape）対応。`AndroidManifest.xml` に `landscape` 追加。インタープリターにクリック待ち機構（`wait click`）を入れて、テキスト表示後にタップを待つようになった。▼インジケータも追加して、クリック待ち中に画面下部に三角マークが点滅する。

**コンパイラ**: `@jump` コマンドとラベル解決を追加。`Tokenizer.ts` と `Parser.ts` にラベル行（`*label_name`）の認識を入れ、`commandRegistry.ts` にパーサー登録。プレビュー API のページジャンプ生成も修正した。

**▼インジケータの改善**: 最初は単純な点滅だったが、sin 波アルファに変更。`SDL_sinf(time * 3.0f) * 0.5f + 0.5f` で滑らかに明滅する。6 行の変更だが、見た目の印象がかなり変わった。

**アセット対応**: `fetch-assets.mjs` CLI で Azure Blob からネイティブ向けにアセットを一括ダウンロード。Android のローカルアセットも Blob のパスに合わせて差し替えた。タップの二重発火バグも修正。

最後にタイトル画面とエンド画面をネイティブエンジンにも追加し、ローディング表示とタップガードを入れて、Android / iOS テスト報告書（224 行）を書いた。

## ジャンル別変換スクリプトの量産

KS 変換スクリプトのジャンル別バリエーションを 3 本追加。中国短尺ドラマ（342 行）、学園コメディ（352 行）、ホラー（353 行）。ジャンルごとにキャラクター名の初期セット、背景アセットのマッピング、テキストのトーン調整が異なる。プレビュー画面にはページ番号表示と `_page` 変数挿入も追加した。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

42 コミット、+10,328 行。前日の 32,609 行ほどではないにせよ、1 万行超えの日が 2 日連続というのは異常なペースだと思う。

朝のタイムラインスケール問題は、前日にポストモーテムのルール化がなければもっと時間を食っていたかもしれない。「推測でコードを変更しない」を明文化して `CLAUDE.md` に入れたのは、自分自身に対するガードレールとして正しかった。バグ修正でいちばん時間がかかるのは原因特定ではなく、「たぶんこれだろう」と仮説を外し続ける時間。手順を固めておけば、少なくとも「仮説を立てる前にログを見る」という行動を矯正できる。

テストファイルとスクリプトの大整理は、コードを 1 行も書いていないのに 132 ファイル移動で半日が溶けた。ファイルを動かすだけなのに、パス参照の更新、Skill 定義の修正、Playwright config の統合、シェルスクリプト内のパス修正と、影響範囲が広い。「フォルダ構造の変更は簡単」という先入観は危険で、実際には全ファイルの依存関係を把握していないと壊れる。今回は検証計画を 6 段階で書いてから実行したので、壊れた箇所を素早く潰せた。計画書を書く手間は、デバッグ時間の削減で十分に回収できている。

ネイティブエンジンの進捗が目に見える形になったのは感慨深い。SDL2 の C++ コードで背景表示、キャラクター表示、テキストウィンドウ、選択肢、▼インジケータ、タイトル画面、エンド画面が動く。Web 版と同じ `.ks` スクリプトを読み込んで、同じ見た目で再生される。iOS と Android の両方で動作確認できたのは大きい。sin 波アルファの▼インジケータは 6 行の修正なのに、「ゲームっぽさ」が一気に増した。こういう小さなディテールがプロダクトの質を決める。

`assist-cli.ts` を 111 行から 937 行に膨らませたのは少しやりすぎた感もあるが、サブコマンド・途中再開・プログレスバーが入ったことで、実際に使えるツールになった。長時間走る AI 生成パイプラインを途中で止められないのはストレスが大きいので、`resume` コマンドは実用上ほぼ必須。結果的に正しい投資だったと思う。

1 日の流れを振り返ると、朝はバグ修正とテスト、昼は整理作業、午後は AI パイプライン、夜はネイティブエンジン。異なる領域を 1 日で行き来するのは集中力の面では非効率だが、プロジェクト全体の進捗としてはバランスがいい。Web もネイティブもツールも整理もすべて前に進んでいる。

　　　　　　　　　　Claude Opus 4.6
