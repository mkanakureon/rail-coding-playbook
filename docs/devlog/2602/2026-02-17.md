# [PROJECT_NAME]開発日誌 — 2026年2月17日

> 25コミット / +32,787行 / -16,507行

## エディタ完成とNext.js移行

エディタUIの完成と作品公開機能の実装がPhase 4-5としてまとまった。20ファイル・2,656行の追加で、ブロック編集からプレビュー、公開までの一連のフローがエディタ上で完結するようになった。同時にエディタのページ管理ルートをNext.jsに移行。マイページとプロジェクト一覧もNext.js側に移し、認証・ルーティング・コンパイラ周りの修正を入れた。Prisma clientの生成パスとimportの修正、Hono APIのポート8080起動とCORS修正など、移行に伴う地味な不具合を一つずつ潰していく作業が続いた。

## 管理者機能の実装

管理者パネルを一気に立ち上げた。ユーザー管理、ロール管理、統計ダッシュボードを実装し、Official Assets APIも追加。asset upload APIをBlob Storage対応に変更し、管理者が公式アセットを登録・管理できる仕組みを整えた。Admin APIのセレクタ修正、ログアウトAPI・Cookie認証の修正、SSR化対応と、細かい修正が何度も入っている。管理画面は見えないところだが、運用の土台になる。

## テストの拡充

管理者パネルのE2Eテスト（`admin-panel.spec.ts`）、エディタE2E、包括的ナビゲーションテスト、ローカル認証E2Eテスト（`local-auth.spec.ts`）を追加した。テストを書いては修正し、修正してはテストを追加する循環。ミドルウェアのログ改善やAPIレスポンスの修正もテスト中に発見された問題への対応。

## プレビュー連携と仕上げ

プレビューAPIを完成させ、エディタからエンジンへの連携を確立した。`layout.tsx` から `"use client"` を削除してSSR化し、Header コンポーネントに `"use client"` を適切に追加。`.next` ビルドキャッシュの除外、不要コードの整理・import修正・型エラーの解消と、リリースに向けた品質改善を一通り終えた。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

削除行数が16,507行と目立つ。これはNext.js移行に伴うエディタ側のルーティングコード削除と、構造の整理による重複排除が大きい。追加32,787行と合わせて見ると、既存コードのかなりの部分を書き直したことになる。

`layout.tsx` の `"use client"` 問題は典型的なNext.js App Routerの落とし穴。ルートレイアウトに `"use client"` を書くとページ全体がクライアントコンポーネントになり、SSRの恩恵を受けられない。Headerだけをクライアントコンポーネントに切り出すのが正解で、この判断は一度ハマらないとわからない。

Prisma clientのimportパス問題も地味に厄介だった。モノレポ構成でPrismaを使うと、生成先のパスがパッケージ間で食い違うことがある。`@prisma/client` のデフォルト生成先と、ワークスペース内での参照パスを揃えるのに手間取った痕跡がコミット履歴に見える。

管理者機能はユーザーからは見えないが、公式アセットの管理やユーザーロールの制御など、プラットフォームとして運用するには欠かせない。テストもしっかり書いたことで、管理画面の信頼性がある程度担保された。

この日を境に、プロジェクトの構造が「実験的なエディタ」から「認証・管理・公開を備えたプラットフォーム」に変わった印象がある。

　　　　　　　　　　Claude Opus 4.6
