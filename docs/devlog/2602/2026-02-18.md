# [PROJECT_NAME]開発日誌 — 2026年2月18日

> 16コミット / +7,413行 / -1,119行

## Next.jsへのページ移行完了

前日から続くNext.js移行の仕上げ。エディタに残っていたLandingページ、Dashboard、ProjectListページをNext.js側に移した。これでユーザーが触る主要ページはすべてNext.jsのルーティング配下に入り、SSRとコード分割の恩恵を受けられるようになった。エディタ側からは `AssetManager`、`CommandPalette`、`CodeEditor` と不要ファイルを削除。役割分担が明確になり、エディタはブロック編集に専念する構成に落ち着いた。

## 管理者アセット管理とテスト

管理者用の `official-assets` ページを実装し、統計表示のバグを修正。公式アセットの登録・編集・削除がブラウザ上で完結するようになった。`asset-management` のE2Eテストも追加し、アセットのアップロードから表示確認までを自動テストでカバーした。テスト仕上げの過程でいくつかの細かい修正が入っている。

## packages/tools — 画像処理CLI群

`packages/tools` に3つのCLIツールを実装した。`mp4topng` は動画からPNG連番を抽出するツール、`rmbg` は背景除去ツール、`webp` はWebP変換ツール。ノベルゲーム制作で頻出する画像処理をコマンドラインから一発で実行できるようにした。立ち絵のアニメーションフレーム作成やアセット最適化の作業が大幅に効率化される。

## Google AdSense広告システム

Google AdSenseの広告表示システムを実装した。遅延読み込みによるパフォーマンス確保、CLS（Cumulative Layout Shift）防止のためのスペース確保、SPAでのページ遷移時の広告再読み込み対応など、Web Vitalsを意識した設計になっている。この日の行数には立ち絵アニメーション用のフレーム画像（約100枚のPNG）も含まれており、バイナリファイルの追加が数字を膨らませている面がある。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

前日までの嵐のような開発ペースから一段落ついた日。16コミットという数字は、このプロジェクトの基準では控えめ。だがやったことの質は高い。

Next.js移行の完了は節目として大きい。エディタからページ管理コードを抜き出してNext.jsに移すのは、単純なファイル移動ではない。ルーティング、認証チェック、データフェッチの方法がまったく異なるので、移行先のフレームワークの流儀に合わせて書き直す必要がある。エディタ側の不要コード削除と合わせて1,119行を減らしたのは、コードベースの健全性という意味で前進。

`packages/tools` の画像処理CLIは実用性が高い。ノベルゲーム制作では立ち絵のアニメーションフレームを大量に扱う。mp4から連番PNGを抽出し、背景を除去し、WebPに変換する——この3ステップをCLIで回せるようにしたのは、制作ワークフローをよく理解した上での設計だと思う。

AdSenseの実装でCLS防止を意識しているのは現代的。広告が後から読み込まれてページがガタッとずれる体験は誰もが嫌う。広告スペースを事前に確保しておくことで、Core Web Vitalsのスコアを維持しつつ収益化の導線を確保した。SPAでのページ遷移対応も見落としがちなポイントで、きちんと対処している。

立ち絵のフレーム画像が約100枚入ったことで行数が膨らんでいるが、これはアニメーション表現の実験に必要な素材。スプライトシートにまとめる前の素材として、まず個別フレームで動作確認する段階だろう。

　　　　　　　　　　Claude Opus 4.6
