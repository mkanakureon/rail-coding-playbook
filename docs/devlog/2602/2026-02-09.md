# [PROJECT_NAME]開発日誌 — 2026年2月9日

> 1コミット / +2,892行 / -11行

## ブラウザ統合 — WebEngine の誕生

この日はたった1コミット。だが中身は重い。`WebEngine.ts` を中心に13ファイル・2,892行で、ブラウザ統合の Phase 1-2 を完了した。`IEngineAPI` インターフェースの Web 実装として384行の `WebEngine` クラスが書かれ、`ksc-demo.html` と `ksc-demo.ts` でデモが動くようになった。

背景表示、キャラクター表示、台詞、選択肢、変数管理、デバッグモード。ビジュアルノベルエンジンとしての基本機能がブラウザ上で一通り動く状態になった。前日までに作ったコンパイラが `.ks` ファイルを Op 配列に変換し、この日作った WebEngine がそれを実行する。パイプラインが繋がった瞬間。

コンパイラからレンダラまでの縦の線が通ると、横に広げる作業——演出の追加、UI の改善、アセットの充実——がすべて具体的になる。1コミットでもプロジェクトのフェーズが変わることがある。この日がまさにそうだった。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

1コミットだけの日。前2日が12コミット、15コミットだったのと対照的に静かだが、技術的にはむしろこの日が一番重要かもしれない。コンパイラとランタイムが繋がって「スクリプトを書けばゲームが動く」状態になった。

`WebEngine.ts` の384行には、PixiJS のステージ管理、テキスト表示、キャラクタースプライトの配置、選択肢 UI の生成、変数ストアの操作が全部詰まっている。384行でこれだけの機能が入るのは、前日までに Op 型を統一してランタイムをシンプルにしたおかげ。設計判断が翌日に効いている好例。

デモページ `ksc-demo.html` の存在は開発体験に大きく影響する。ターミナルでコンパイルして、ブラウザをリロードすれば結果が見える。このフィードバックループの短さが、以降の開発速度を支えることになる。

`playTimeline()` が未実装のまま残っているのと、BGM/SE ファイルが存在しないという既知の課題がある。音が出ないビジュアルノベルは寂しいが、まずは目に見えるものから固めるという優先順位は正しい。音は後から足せるが、テキスト表示やキャラクター配置のタイミング制御は後回しにすると破綻する。

この日は「量より質」というよりも「量は必要十分だけ、質は高く」という日だった。2,892行のうち無駄なコードがほとんどない。前日までの試行錯誤を経て、何を作るべきかが明確になっていたのだろう。

　　　　　　　　　　Claude Opus 4.6
