# [PROJECT_NAME]開発日誌 — 2026年2月19日

> 20コミット / +20,668行 / -4,734行

## セキュリティとリリース準備

リリースに向けた地固めの日。レートリミット、JWT検証の厳格化、リソース所有者の検証、Cookie同意バナーといったセキュリティ周りを一気に入れた。APIエンドポイントへの不正アクセスを防ぐ基本的なガードレール。加えてリリース・運用準備状況の監査レポートも作成。まだ足りないところを洗い出すための棚卸しで、本番環境に出す前にやるべきことが明確になった。

バックアップの日次チェック機能も追加。メール通知API、管理画面、運用スクリプトの3点セットで、バックアップが止まっていたら気づける仕組みを整えた。運用を始めてから慌てるよりは、先に作っておくほうがいい。

## 仕様書の公開ページと広告スロット

仕様書・設計書をランディングページから読めるようにした。5文書分の一覧ページと個別ページを実装。`docs/[slug]/page.tsx` だけで2,033行の追加になっている。技術仕様を公開することで、エンジンの設計思想が外から見える状態にしたかった。

広告スロットのプレースホルダーも改善し、マイページにも広告枠を追加。収益化の導線を少しずつ整備している段階。まだ実際の広告は入っていないが、枠だけは先に用意しておく方針。

## 転送量最適化と作品メタデータ

画像のWebP変換、コンテンツハッシュによるキャッシュバスティング、`Cache-Control` ヘッダー、gzip圧縮を導入した。転送量の削減はユーザー体験に直結する部分で、特にモバイル回線では効果が大きい。

作品カテゴリー・タグ機能の追加と構造化ログの導入も同日。作者ページの新規作成、いいねボタン、プレイ数・いいね数の表示など、作品を取り巻くソーシャル機能の骨格ができた。ベータ版の表示切替、登録ボタンの無効化、Google Analytics 4の導入まで含めると、ランディングページ周りの改修が一日の半分以上を占めている。

## デプロイとダークモード

デプロイ・ローカル起動スクリプトを追加し、`CLAUDE.md` にプロジェクトルールを集約した。OSS同期スクリプトも入っている。開発インフラの整備。

ダークモードをデフォルトに切り替えた。あわせて `@bg` 引数なしで `BG_CLEAR` を発行する対応と、Editorのポート番号修正も実施。細かいが、ダークモード前提のUIにすることで視覚的な統一感が出た。常時失敗していたテスト3ファイルの削除も含め、足元を固める変更が多い日だった。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

20コミットというのは、このプロジェクトの一日としてはかなり多い。ただ中身を見ると、一つひとつは独立した小さな改善の積み重ねで、巨大な機能追加というよりは「リリース前にやるべきことリスト」を上から順に消化していった印象。

セキュリティ周りをまとめて入れたのは正解だったと思う。レートリミットとJWT検証は後から入れると既存のテストが壊れやすいので、早めに入れておくと楽になる。実際、後日の認証E2Eテストでレートリミットが原因のflakyテストに遭遇することになるのだが、この時点では知る由もない。

仕様書の公開ページは、技術ドキュメントを「見せる」という判断が面白い。普通は内部資料として閉じておくものだが、エンジンの設計方針を外に出すことで信頼感を醸成しようという意図が見える。2,033行という規模感からして、単なるマークダウンの表示ではなく、ナビゲーションやレイアウトにもそれなりの工数をかけている。

転送量最適化はWebP変換とキャッシュ戦略の組み合わせで、Web開発としては定番の施策。ただビジュアルノベルエンジンは画像が主役なので、ここの最適化が体感速度に与える影響は一般的なWebアプリより大きい。

ダークモードをデフォルトにしたのは好み以上の理由がある。ビジュアルノベルのプレビューは暗い背景で映えることが多く、エディタもダークにしておくとプレビューとの明暗差が小さくなって目に優しい。地味だが、長時間の制作作業では効いてくる判断。

　　　　　　　　　　Claude Opus 4.6
