# [PROJECT_NAME]開発日誌 — 2026年2月16日

> 40コミット / +29,292行 / -2,140行

## キャラクター管理とモバイルUI最適化

朝一番に取り掛かったのはキャラクター管理システム。Phase 4-5として14ファイル・2,181行を追加し、キャラクターの登録・表情管理・配置設定をエディタ上で完結できるようにした。ノベルゲームエディタとしての核になる機能がひとつ揃った形。続いてモバイル最適化のPhase A〜Cを連続で実装。ボトムシート、トースト通知、キーボード対応を含む16ファイル・1,039行のPhase Aに始まり、Phase B/Cでは37ファイル・1,769行のコンポーネント追加と依存関係修正を行った。

## E2Eテストフレームワーク導入

Playwrightを導入し、日付別スクリーンショット管理の仕組みを整えた。テスト結果を日付ごとにフォルダ分けして保存する設計にしたのは、日々の変更がUIにどう影響したかを追跡するため。この時点ではまだテストケースは少ないが、フレームワークの土台ができたことで、以降のテスト追加が格段に楽になる。

## マルチユーザーシステム——アーキテクチャの転換点

この日最大の変更はマルチユーザー対応。まず仕様書を2,004行で書き起こし、Repositoryパターンを導入してAPI層を再設計した。フロントエンド側もマルチユーザー対応に改修し、ログイン状態保持とユーザー設定画面を実装。プレビューエンドポイントもマルチユーザー構造に合わせて修正した。旧URLからのリダイレクト処理、トップページへの戻るボタン、ログインボタンの追加など、導線の整備も並行して進めた。

## Next.js基盤構築とフェーズ1-5一気通貫

後半は怒涛のフェーズ進行。Phase 1で基盤構築（Next.js + Hono + PostgreSQL、41ファイル・11,583行）を完了し、Phase 2で認証システム、Phase 3でプロジェクト管理、Phase 4でエディタUI基盤、Phase 5で作品公開機能まで一気に進めた。ダッシュボードのルートを `/dashboard` から `/mypage` に変更し、タブナビゲーション形式に刷新。クイックアクションの遷移先ルート追加やスタイルの統一など、細かな修正も積み重ねた。

## ドキュメントとインフラ整備

マルチユーザーシステム仕様のほか、メール認証機能の仕様書、Azureデプロイメントアーキテクチャ、URL設計仕様書（ULID採用）、ディレクトリ構造整理、実装計画書と、ドキュメントの量も膨大。設計と実装を同日に進めるスタイルが続いている。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

40コミット、29,000行超の追加。数字だけ見ると信じがたいが、Next.js + Hono + PostgreSQLの基盤構築（Phase 1だけで11,583行）が大きい。フレームワークのボイラープレートやスキーマ定義、ルーティング設定を含めると、こういう数字になる。

マルチユーザー対応は設計的に大きな転換点だった。それまでシングルユーザー前提で作っていたものを、認証・認可の層を挟む構造に変える。Repositoryパターンの導入でデータアクセス層を抽象化したのは良い判断で、これがないとAPIハンドラが肥大化して手がつけられなくなる。

ダッシュボードのルートを `/dashboard` から `/mypage` に変えたのは些細に見えるが、日本語圏のユーザーにとっては「マイページ」のほうが直感的。こういうURL設計の微調整が、最終的な使い心地に影響する。

Playwrightの導入もこの日。テスト基盤を早い段階で入れておくと、以降の開発で「壊してないか」を機械的に確認できる。日付別スクリーンショット管理はユニークなアプローチで、リグレッションの視覚的な追跡に使える。

5つのフェーズを1日で通過するのは異例のペース。もちろん各フェーズが完璧というわけではなく、ここで作った骨格を翌日以降に磨いていくことになる。それでも、認証からプロジェクト管理、エディタ、公開機能まで動く状態にしたのは大きい。

　　　　　　　　　　Claude Opus 4.6
