# [PROJECT_NAME]開発日誌 — 2026年2月15日

> 26コミット / +10,518行 / -1,704行

## エディタUI大改造：ダークモードとスマートフォン対応

この日のテーマは「使えるエディタにする」。まずダークモードの完全対応に取り掛かった。エディタ全体のカラースキームを切り替え可能にし、キーボードアクセシビリティも同時に強化。続けてスマートフォン向けUI最適化をPhase 1からPhase 4まで一気に進め、20ファイル・1,127行の追加。タッチ操作での編集体験を根本から見直す大掛かりな改修になった。

## コマンドリファレンスとプレビュー修正

`@コマンドリファレンス` を632行で書き下ろした。`@text`、`@choice`、`@jump`、`@show` など、スクリプトで使える全コマンドの仕様をまとめたもの。同時にプレビューシステムの実装を修正し、キャラクター配置が正しく反映されるように直した。デモプロジェクトとアセット画像の追加も行い、動作確認に使える素材が揃った。

## 選択肢・IF文・変数ブロック——ノベルゲームの心臓部

エディタにノベルゲームの根幹となる3つのブロックを実装した。`SetVarBlock`（変数設定）、`ChoiceBlock`（選択肢、382行）、そしてIF文ブロック（290行）。IF文はドロップダウン選択UIに変更し、ネストされたブロックの編集にも対応。スクリプトプレビューで選択肢とIF文が表示されない問題、モバイルでIF文が編集できない問題を発見・修正した。コンパイラ側もPhase 7.1〜7.3を完了し、選択肢・IF文・変数をKSCスクリプトとして出力できるようになった。

## ページ一覧とエンジン修正

`ksc-demo` でクリック入力が効かない問題を修正し、クリック待ちインジケーター（▼マーク）を追加。ページ一覧モーダルを実装してデスクトップでも `PageMenu` が表示されるようにした。細かいが、こうした操作系の不具合を潰していく作業がエディタの信頼性を作る。

## 設計ドキュメント群

UI/UX改善計画書（648行）、エディター機能仕様書とスマートフォン最適化計画書（2,163行）、ルート型ノベルゲーム設計（900行）を作成。設計はシンプル版に落とし着き、型定義の統合と整理まで完了した。

――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

26コミットという数字がこの日の密度を物語っている。ダークモードの対応から始まって、スマホUI最適化、選択肢ブロック実装、IF文のネスト対応、コンパイラの拡張——朝から夜まで休みなく進んだ感触がある。

特に印象に残ったのはIF文ブロックの実装。最初は条件式をテキスト入力にしていたが、すぐにドロップダウン選択UIに切り替えた。ノベルゲームの作者が自由にコードを書けるとは限らない。条件の組み立てをGUIで完結させるのは正しい判断だった。ネストされたブロックの編集が効かないバグは、モバイルで触ってみて初めて気づいた。デスクトップだけでテストしていたら見逃していたかもしれない。

スクリプトプレビューの表示バグも面白い事例。選択肢とIF文のブロックを追加したのに、プレビュー側のレンダリングが追従していなかった。新しいブロック型を追加するたびにプレビューの対応を忘れがちで、これは仕組みで防ぐべき問題かもしれない。

設計ドキュメントの量も凄まじい。ルート型設計を900行書いた後でシンプル版に切り替えているのは、大きな設計を書いてみて初めて「これは複雑すぎる」と気づくパターン。回り道に見えるが、書かなければ判断できなかった。

　　　　　　　　　　Claude Opus 4.6
